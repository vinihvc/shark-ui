---
title: Approval Card
description: A composable card for agent permissions, plan reviews, and clarification flows.
---

<Alert>
  <InfoIcon />
  <AlertDescription>
  Approval Card is built on top of the [Questionnaire](/docs/components/questionnaire) component. Check it out for more examples.
  </AlertDescription>
</Alert>

<ComponentPreview 
  componentName="approval-card" 
  fileName="example-default" autoHeight
  className="**:data-[slot=preview]:h-[540px]"
/>

## Installation

<CodeTabs>

<TabsList>
<TabsTrigger value="cli">CLI</TabsTrigger>
<TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add @shark/approval-card
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install [Button](/docs/components/button), [Card](/docs/components/card), and [Questionnaire](/docs/components/questionnaire).</Step>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource title="components/ui/approval-card.tsx" src="/approval-card.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Anatomy

```text
ApprovalCard
├── ApprovalCardHeader
│   ├── ApprovalCardTitle
│   └── ApprovalCardAction
│       └── ApprovalCardProgress
├── ApprovalCardContent
│   └── ApprovalCardItem
│       ├── ApprovalCardItemTitle
│       ├── ApprovalCardItemDescription
│       ├── ApprovalCardChoices
│       │   ├── ApprovalCardChoice
│       │   │   └── ApprovalCardChoiceShortcut
│       │   └── ApprovalCardInput
│       └── ApprovalCardError
└── ApprovalCardFooter
    ├── ApprovalCardPrevious
    ├── ApprovalCardSkip
    ├── ApprovalCardReject
    ├── ApprovalCardNext
    └── ApprovalCardSubmit
```

## Usage

```tsx
import {
  ApprovalCard,
  ApprovalCardAction,
  ApprovalCardChoice,
  ApprovalCardChoices,
  ApprovalCardContent,
  ApprovalCardError,
  ApprovalCardFooter,
  ApprovalCardHeader,
  ApprovalCardItem,
  ApprovalCardItemTitle,
  ApprovalCardProgress,
  ApprovalCardReject,
  ApprovalCardSubmit,
  ApprovalCardTitle,
} from "@/components/ui/approval-card";
```

## Title

`ApprovalCardHeader` supports two title patterns:

### Using props

Pass the `title` prop directly to `ApprovalCardHeader`.

```tsx showLineNumbers
<ApprovalCardHeader title="Apply the staging migration?">
  <TerminalIcon aria-hidden="true" />
  <ApprovalCardAction>
    <ApprovalCardProgress />
  </ApprovalCardAction>
</ApprovalCardHeader>
```

> This approach does not require the `ApprovalCardTitle` component.

### Using components

Use `ApprovalCardTitle` as a child for more control.

```tsx showLineNumbers
<ApprovalCardHeader>
  <TerminalIcon aria-hidden="true" />
  <ApprovalCardTitle>Apply the staging migration?</ApprovalCardTitle>
  <ApprovalCardAction>
    <ApprovalCardProgress />
  </ApprovalCardAction>
</ApprovalCardHeader>
```

## Controlled

Control the active item and selected values when the surrounding application owns the flow state.

<ComponentPreview componentName="approval-card" fileName="example-controlled" autoHeight />

## States

### Invalid

Render validation feedback after a required approval choice has not been made.

<ComponentPreview componentName="approval-card" fileName="example-invalid" autoHeight />

### Disabled

Disable unavailable choices and freeform input while preserving available paths.

<ComponentPreview componentName="approval-card" fileName="example-disabled" autoHeight />

## Examples

### With close button

Place a dismiss action beside the title with `ApprovalCardAction`. The
surrounding application owns the card's open state.

<ComponentPreview componentName="approval-card" fileName="example-with-close-button" autoHeight />

### Custom spacing

The `[--space:--spacing("value")]` custom property from `Card` controls the Approval Card's internal spacing.

Default spacing is `--spacing(6)`.

<ComponentPreview componentName="approval-card" fileName="example-custom-spacing" autoHeight />

> You can use breakpoint utilities to change the internal spacing at different screen sizes.

### Command execution

Show the command, working directory, target environment, and expected effect before asking for approval.

<ComponentPreview componentName="approval-card" fileName="example-command" autoHeight />

### Permission scope

Use a required answer to capture whether approval applies once or to the current repository.

<ComponentPreview componentName="approval-card" fileName="example-permission-scope" autoHeight />

### Plan review

Compose [Plan](/docs/ai-elements/plan) with Approval Card to review an approach before implementation.

<ComponentPreview componentName="approval-card" fileName="example-plan" autoHeight />

### File edit

Compose [Diff](/docs/ai-elements/diff) with Approval Card to show the proposed change before applying it.

<ComponentPreview componentName="approval-card" fileName="example-file-edit" autoHeight />

### MCP tool call

Show the server, tool name, arguments, and external side effects before approving a call.

<ComponentPreview componentName="approval-card" fileName="example-mcp-tool" autoHeight />

### Network access

Ask for access to a specific network destination and scope before making a request.

<ComponentPreview componentName="approval-card" fileName="example-network-access" autoHeight />

### Clarifying requirements

Use multiple steps and a freeform alternative to gather requirements before drafting a plan.

<ComponentPreview componentName="approval-card" fileName="example-clarification" autoHeight />

## API Reference

### ApprovalCard

Form root.

| Prop | Type | Default |
| --- | --- | --- |
| `items` | `readonly ApprovalCardItemDefinition[]` | `[{ name: "approval" }]` |
| `item` | `string` | `-` |
| `defaultItem` | `string` | `-` |
| `onItemChange` | `(details: ApprovalCardItemChangeDetails) => void` | `-` |
| `value` | `ApprovalCardValue` | `-` |
| `defaultValue` | `ApprovalCardValue` | `{}` |
| `onValueChange` | `(details: ApprovalCardValueChangeDetails) => void` | `-` |
| `shortcuts` | `"letters" \| "numbers"` | `-` |
| `onSubmit` | `FormEventHandler<HTMLFormElement>` | `-` |
| `onApprove` | `(details?: ApprovalCardSubmitDetails) => void` | `-` |
| `onReject` | `() => void` | `-` |
| `className` | `string` | `-` |

### ApprovalCardHeader

Header row for the icon, title, and optional action.

| Prop | Type | Default |
| --- | --- | --- |
| `title` | `string` | `-` |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### ApprovalCardTitle

Card title.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### ApprovalCardAction

Aligns a header action, such as progress, at the end.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### ApprovalCardProgress

Current question progress.

| Prop | Type | Default |
| --- | --- | --- |
| `aria-label` | `string` | `"Question progress"` |
| `className` | `string` | `-` |

### ApprovalCardContent

Main body.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### ApprovalCardItem

One question step.

| Prop | Type | Default |
| --- | --- | --- |
| `name` | `string` | **required** |
| `id` | `string` | generated |
| `className` | `string` | `-` |

### ApprovalCardItemTitle

Question title.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |

### ApprovalCardItemDescription

Supporting question text.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |

### ApprovalCardChoices

Container for radio or checkbox choices.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |

### ApprovalCardChoice

One selectable answer.

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `string` | **required** |
| `disabled` | `boolean` | `false` |
| `className` | `string` | `-` |

### ApprovalCardChoiceShortcut

Shortcut badge for a choice.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |

### ApprovalCardInput

Freeform text answer.

| Prop | Type | Default |
| --- | --- | --- |
| `placeholder` | `string` | `-` |
| `disabled` | `boolean` | `false` |
| `onChange` | `ChangeEventHandler<HTMLInputElement>` | `-` |
| `className` | `string` | `-` |

### ApprovalCardError

Validation message for the active question.

| Prop | Type | Default |
| --- | --- | --- |
| `children` | `ReactNode` | `"Choose an answer to continue."` |
| `className` | `string` | `-` |

### ApprovalCardFooter

Footer for navigation and approve or reject.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |

### ApprovalCardPrevious

Returns to the previous question. Default label is `Previous`.

| Prop | Type | Default |
| --- | --- | --- |
| `disabled` | `boolean` | `false` |
| `onClick` | `MouseEventHandler<HTMLButtonElement>` | `-` |
| `className` | `string` | `-` |

### ApprovalCardNext

Validates and advances to the next question. Default label is `Next`.

| Prop | Type | Default |
| --- | --- | --- |
| `disabled` | `boolean` | `false` |
| `onClick` | `MouseEventHandler<HTMLButtonElement>` | `-` |
| `className` | `string` | `-` |

### ApprovalCardSkip

Skips an optional question. Default label is `Skip`.

| Prop | Type | Default |
| --- | --- | --- |
| `onClick` | `MouseEventHandler<HTMLButtonElement>` | `-` |
| `className` | `string` | `-` |

### ApprovalCardSubmit

Submits the final question. Default label is `Approve`.

| Prop | Type | Default |
| --- | --- | --- |
| `disabled` | `boolean` | `false` |
| `className` | `string` | `-` |

### ApprovalCardReject

Calls `onReject()`. Default label is `Reject`.

| Prop | Type | Default |
| --- | --- | --- |
| `variant` | `"default" \| "outline" \| "secondary" \| "ghost" \| "link" \| "destructive"` | `"ghost"` |
| `size` | `"xs" \| "sm" \| "md" \| "lg" \| "xl" \| "icon-xs" \| "icon-sm" \| "icon-md" \| "icon-lg" \| "icon-xl"` | `"md"` |
| `className` | `string` | `-` |
| `asChild` | `boolean` | `false` |
