---
title: Button
description: A button or an element styled as a button.
---

import { ComponentPreviewFile } from "@/components/component-preview-file";
import { ComponentVariants } from "@/components/component-variants";
import { TabsContent } from "@/registry/react/components/tabs";
import { Button } from "@/registry/react/components/button";
import { ComponentInstallation } from "@/components/component-installation";
import { Plus } from "lucide-react";

<ComponentPreviewFile componentName="button" />

## Installation

<ComponentInstallation componentName="button">

Install the following dependencies:
  
```bash
pnpm add @ark-ui/react tailwind-variants
```
</ComponentInstallation>

## Usage

```tsx lineNumbers
import { Button } from "@/components/ui/button";
```

```tsx lineNumbers
<Button>Button</Button>
```

## Link

You can use [asChild](/docs/as-child) prop on `<Button />` to make another element styled as a button.

<ComponentPreviewFile componentName="button" fileName="state-as-child" />

## Variants

You can change the variant of the button using the `variant` prop.

Available variants are: `solid`, `outline`, `success`, `info`, `warning`, `destructive`.

### Solid

<ComponentPreviewFile componentName="button" fileName="variant-solid" />

### Outline

<ComponentPreviewFile componentName="button" fileName="variant-outline" />

### Secondary

<ComponentPreviewFile componentName="button" fileName="variant-secondary" />

### Ghost

<ComponentPreviewFile componentName="button" fileName="variant-ghost" />

### Link

<ComponentPreviewFile componentName="button" fileName="variant-link" />

### Destructive

<ComponentPreviewFile componentName="button" fileName="variant-destructive" />

## Sizes

You can change the size of the button using the `size` prop.

Available sizes are: `xs`, `sm`, `md`, `lg`, `icon-xs`, `icon-sm`, `icon-md`, `icon-lg`.

### Extra Small

<ComponentPreviewFile componentName="button" fileName="size-xs" />

### Small

<ComponentPreviewFile componentName="button" fileName="size-sm" />

### Medium

<ComponentPreviewFile componentName="button" fileName="size-md" />

### Large

<ComponentPreviewFile componentName="button" fileName="size-lg" />

### Extra Large

<ComponentPreviewFile componentName="button" fileName="size-xl" />

## Disabling click effect

You can disable the click effect of the button using the `clickEffect` prop.

<ComponentPreviewFile componentName="button" fileName="state-click-effect-false" />


## Loading

You can show a loading indicator when the button is loading using the `isLoading` prop.

Available loading states are: `true`, `false`.

<ComponentPreviewFile componentName="button" fileName="state-loading-true" />

## API Reference

| Prop | Type | Default | 
| --- | --- | --- | 
| `variant` | `solid`, `outline`, `secondary`, `ghost`, `link`, `destructive` | `solid` | 
| `size` | `xs`, `sm`, `md`, `lg`, `xl`, `icon-xs`, `icon-sm`, `icon-md`, `icon-lg`, `icon-xl` | `md` | 
| `clickEffect` | `boolean` | `true` | 
| `isLoading` | `boolean` | `false` | 
