---
title: Accordion
description: A collapsible for displaying content.
links:
  doc: https://ark-ui.com/docs/components/accordion
  api: https://ark-ui.com/docs/components/accordion#api-reference
---

<ComponentPreviewFile componentName="accordion" />

## Installation

<ComponentInstallation componentName="accordion" >
Install the following dependencies:
  
```bash
pnpm add @ark-ui/react lucide-react
```
</ComponentInstallation>

## Usage

```tsx lineNumbers
import { 
  Accordion, 
  AccordionItem, 
  AccordionTrigger, 
  AccordionContent 
} from "@/components/ui/accordion";
```

```tsx lineNumbers
<Accordion>
  <AccordionItem value="item-1">
    <AccordionTrigger>Accordion Item 1</AccordionTrigger>
    <AccordionContent>Accordion Content 1</AccordionContent>
  </AccordionItem>

  <AccordionItem value="item-2">
    <AccordionTrigger>Accordion Item 2</AccordionTrigger>
    <AccordionContent>Accordion Content 2</AccordionContent>
  </AccordionItem>
</Accordion>
```

## Examples

### Single

By default, only one item can be open at a time. Use the `collapsible` prop to allow closing the open item.

<ComponentPreviewFile componentName="accordion" fileName="single" />

### Multiple

Use the `multiple` prop to allow multiple items to be open simultaneously.

<ComponentPreviewFile componentName="accordion" fileName="multiple" />

### Controlled

Use the `value` and `onValueChange` props to control the accordion state programmatically.

<ComponentPreviewFile componentName="accordion" fileName="controlled" />

## API Reference

### Accordion

Root element of the accordion. Renders a `div`

| Prop | Type | Default |
|------|------|---------|
| `collapsible` | `boolean` | `false` |
| `multiple` | `boolean` | `false` |
| `value` | `string[]` | `-` |
| `defaultValue` | `string[]` | `-` |
| `onValueChange` | `ValueChangeDetails` | `-` |
| `className` | `string` | `-` |

### AccordionItem

Container for a single accordion item. Renders a `div`

| Prop | Type | Default |
|------|------|---------|
| `value` | `string` | **required** |
| `className` | `string` | `-` |

### AccordionTrigger

Button that toggles the accordion item. Renders a `button`

| Prop | Type | Default |
|------|------|---------|
| `disabled` | `boolean` | `false` |
| `className` | `string` | `-` |

### AccordionContent

Container for the accordion item content. Renders a `div`

| Prop | Type | Default |
|------|------|---------|
| `className` | `string` | `-` |
