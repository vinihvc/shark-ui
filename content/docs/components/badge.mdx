---
title: Badge
description: A badge or an element styled as a badge.
---

<ComponentPreviewFile componentName="badge" />

## Installation

<ComponentInstallation componentName="badge">

Install the following dependencies:
  
```bash
pnpm add @ark-ui/react tailwind-variants
```
</ComponentInstallation>

## Usage

```tsx lineNumbers
import { Badge } from "@/components/ui/badge";
```

```tsx lineNumbers
<Badge>New</Badge>
```

## Variants

You can change the variant of the badge using the `variant` prop.

### Solid

<ComponentPreviewFile componentName="badge" fileName="variant-solid" />

### Secondary

<ComponentPreviewFile componentName="badge" fileName="variant-secondary" />

### Outline

<ComponentPreviewFile componentName="badge" fileName="variant-outline" />

### Success

<ComponentPreviewFile componentName="badge" fileName="variant-success" />

### Info

<ComponentPreviewFile componentName="badge" fileName="variant-info" />

### Warning

<ComponentPreviewFile componentName="badge" fileName="variant-warning" />

### Destructive

<ComponentPreviewFile componentName="badge" fileName="variant-destructive" />

## Sizes

You can change the size of the badge using the `size` prop.

### Small

<ComponentPreviewFile componentName="badge" fileName="size-sm" />

### Medium

<ComponentPreviewFile componentName="badge" fileName="size-md" />

### Large

<ComponentPreviewFile componentName="badge" fileName="size-lg" />

## Pill

You can use the `pill` prop to create fully rounded badges.

<ComponentPreviewFile componentName="badge" fileName="state-pill-true" />

## Examples

### With link

You can use [asChild](/docs/as-child) prop on `<Badge />` to make another element styled as a badge.

<ComponentPreviewFile componentName="badge" fileName="example-with-link" />

### Custom color

You can use the `className` prop to add custom colors to the badge.

<ComponentPreviewFile componentName="badge" fileName="example-custom-color" />

### With icon

You can add icons to badges to provide visual context.

<ComponentPreviewFile componentName="badge" fileName="example-with-icon" />

### With spinner

You can add a `<Spinner />` to badges to indicate loading states.

<ComponentPreviewFile componentName="badge" fileName="example-with-spinner" />

### With count

Badges can display counts or numbers for notifications and indicators.

<ComponentPreviewFile componentName="badge" fileName="example-with-count" />

## API Reference

### Badge

Root element of the badge. Renders a `span`

| Prop | Type | Default | 
| --- | --- | --- | 
| `variant` | `solid`, `secondary`, `outline`, `success`, `info`, `warning`, `destructive` | `solid` | 
| `size` | `sm`, `md`, `lg` | `md` | 
| `className` | `string` | `-` | 

