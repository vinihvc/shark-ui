---
title: Action Bar
description: Display an action bar for selected items.
---

<ComponentPreview componentName="action-bar" />

## Installation

<CodeTabs>
<TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @shark/action-bar
```

</TabsContent>
<TabsContent value="manual">

<Steps>
<Step>This component depends on [Badge](/docs/components/badge) and [Separator](/docs/components/separator). Install them first if you haven't already.</Step>

<Step>Install the following dependencies:</Step>

```bash
npm install @ark-ui/react tailwind-variants
```

<Step>Copy and paste the following code into your project.</Step>

  <ComponentSource title="components/ui/action-bar.tsx" src="/action-bar.tsx" />

<Step>Update the import paths to match your project setup.</Step>
</Steps>

</TabsContent>
</CodeTabs>

## Anatomy

```text
ActionBar
├── ActionBarTrigger
└── ActionBarContent
    ├── ActionBarValue
    ├── ActionBarSeparator
    ├── ActionBarBody
    │   └── Button
    ├── ActionBarSeparator
    └── ActionBarClose
```

## Usage

```tsx
import {
  ArchiveIcon,
  DownloadIcon,
  PencilIcon,
  Trash2Icon,
  XIcon,
} from "lucide-react";
import {
  ActionBar,
  ActionBarBody,
  ActionBarClose,
  ActionBarContent,
  ActionBarSeparator,
  ActionBarTrigger,
  ActionBarValue,
} from "@/components/ui/action-bar";
import { Button } from "@/components/ui/button";
```

```tsx
<ActionBar>
  <ActionBarTrigger asChild>
    <Button variant="outline">Open</Button>
  </ActionBarTrigger>
  <ActionBarContent aria-label="Bulk actions">
    <ActionBarValue count={3} />
    <ActionBarSeparator />
    <ActionBarBody>
      <Button variant="ghost">
        <PencilIcon aria-hidden="true" />
        <span className="max-sm:sr-only">Edit</span>
      </Button>
      <ActionBarSeparator />
      <Button variant="destructive">
        <Trash2Icon aria-hidden="true" />
        <span className="max-sm:sr-only">Delete</span>
      </Button>
    </ActionBarBody>
    <ActionBarSeparator />
    <ActionBarClose asChild>
      <Button size="icon-md" variant="ghost">
        <XIcon aria-hidden="true" />
      </Button>
    </ActionBarClose>
  </ActionBarContent>
</ActionBar>
```

## Controlled

<ComponentPreview componentName="action-bar" fileName="example-controlled" />

## Examples

### Positioning

Use the `positioning.placement` prop to position the action bar.

<ComponentPreview componentName="action-bar" fileName="example-positioning" />

### Gutter

Use `positioning.gutter` to control the distance from the bottom edge.

<ComponentPreview componentName="action-bar" fileName="example-gutter" />

### Close Trigger

<ComponentPreview componentName="action-bar" fileName="example-close-trigger" />

### With Dialog

<ComponentPreview componentName="action-bar" fileName="example-dialog" />

### With Menu

<ComponentPreview componentName="action-bar" fileName="example-menu" />

### Table

<ComponentPreview componentName="action-bar" fileName="example-table" />

### Custom Spacing

The `[--space:--spacing("value")]` on `ActionBarContent` controls the internal spacing.

Default spacing is `--spacing(2)`.

<ComponentPreview componentName="action-bar" fileName="example-custom-spacing" />

> You can use breakpoint utilities to change the internal spacing at different screen sizes.

```css
md:[--space:--spacing(6)] lg:[--space:--spacing(8)]
```

## Accessibility

The action bar uses Ark UI's non-modal Popover with `role="dialog"`. Opening
it leaves focus where it is, and the page remains interactive and scrollable.
Clicking or focusing outside does not close the bar.

Give `ActionBarContent` a descriptive `aria-label` or `aria-labelledby`.
The fallback label is "Bulk actions". Label icon-only controls and hide
decorative icons from assistive technology.

With `ActionBarTrigger`, Tab moves into the open bar and Shift+Tab can return
to the trigger. Tab does not trap focus. Escape closes the topmost overlay
first; `closeOnEscape={false}` disables Escape dismissal for the bar.

Focus restoration follows Ark UI's Popover behavior and uses `ActionBarTrigger`
when available. For selection-driven bars without a trigger, the application
is responsible for placing focus appropriately if it removes a focused control.
Custom action shortcuts should be configured by the application.

## API Reference

### ActionBar

Root component. Provides open state and positioning to descendants.

| Prop | Type | Default |
| --- | --- | --- |
| `open` | `boolean` | - |
| `defaultOpen` | `boolean` | `false` |
| `positioning` | `ActionBarPositioning` | `{ placement: "bottom", gutter: "16px" }` |
| `lazyMount` | `boolean` | `true` |
| `unmountOnExit` | `boolean` | `true` |
| `closeOnEscape` | `boolean` | `true` |
| `onOpenChange` | `(open: boolean) => void` | - |

### ActionBarContent

Non-modal action surface. Renders Ark UI's Popover content with a fixed positioner.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |
| `aria-label` | `string` | `"Bulk actions"` |
| `aria-labelledby` | `string` | - |
| `asChild` | `boolean` | - |

| CSS variable | Default |
| --- | --- |
| `--space` | `--spacing(2)` |

### ActionBarTrigger

Button that toggles the action bar using Ark UI's Popover trigger.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |
| `asChild` | `boolean` | - |

### ActionBarValue

Displays the selection count.

| Prop | Type | Default |
| --- | --- | --- |
| `count` | `number` | - |
| `label` | `string` | - |
| `className` | `string` | - |
| `asChild` | `boolean` | - |

### ActionBarSeparator

Vertical rule. Renders a [Separator](/docs/components/separator).

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |
| `asChild` | `boolean` | - |

### ActionBarBody

Flex row for primary actions.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |
| `asChild` | `boolean` | - |

### ActionBarClose

Closes the action bar using Ark UI's Popover close trigger.

| Prop | Type | Default |
| --- | --- | --- |
| `className` | `string` | - |
| `asChild` | `boolean` | - |
