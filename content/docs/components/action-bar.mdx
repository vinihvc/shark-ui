---
title: Action Bar
description: Display a bottom action bar with a set of actions for selected items.
links:
  doc: https://chakra-ui.com/docs/components/action-bar
---

<ComponentPreview componentName="action-bar" />

## Installation

<CodeTabs>
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">

```bash
npx shadcn@latest add @shark/action-bar
```

  </TabsContent>
  <TabsContent value="manual">

<Steps>
  <Step>Install the following dependencies:</Step>

```bash
npm install @ark-ui/react lucide-react tailwind-variants
```

  <Step>Copy and paste the following code into your project.</Step>

  <ComponentSource title="components/ui/action-bar.tsx" src="/action-bar.tsx" />

  <Step>Update the import paths to match your project setup.</Step>
</Steps>

  </TabsContent>
</CodeTabs>

## Usage

```tsx
import {
  ActionBar,
  ActionBarClose,
  ActionBarContent,
  ActionBarSelectionTrigger,
  ActionBarSeparator,
  ActionBarTrigger,
} from "@/components/ui/action-bar";
```

```tsx
<ActionBar>
  <ActionBarTrigger />
  <ActionBarContent>
    <ActionBarClose />
    <ActionBarSelectionTrigger count={3} />
    {/* Your actions */}
  </ActionBarContent>
</ActionBar>
```

## Examples

### Side

<ComponentPreview componentName="action-bar" fileName="example-side" />

### Table with Selection

<ComponentPreview componentName="action-bar" fileName="example-table" />

## API Reference

### ActionBar

| Prop | Type | Default |
|------|------|---------|
| `open` | `boolean` | - |
| `defaultOpen` | `boolean` | `false` |
| `lazyMount` | `boolean` | `true` |
| `unmountOnExit` | `boolean` | `true` |
| `onOpenChange` | `(open: boolean) => void` | - |

### ActionBarContent

| Prop | Type | Default |
|------|------|---------|
| `placement` | `"bottom" \| "bottom-start" \| "bottom-end"` | `"bottom"` |
| `className` | `string` | - |
| `asChild` | `boolean` | `false` |

### ActionBarPositioner

| Prop | Type | Default |
|------|------|---------|
| `side` | `"bottom" \| "bottom-start" \| "bottom-end"` | `"bottom"` |
| `className` | `string` | - |
| `asChild` | `boolean` | `false` |

### ActionBarTrigger

Opens the action bar when clicked. Use `asChild` to merge props with a child element (e.g. `Button`).

| Prop | Type | Default |
|------|------|---------|
| `className` | `string` | - |
| `asChild` | `boolean` | `false` |

### ActionBarClose

Closes the action bar when clicked. Use `asChild` to merge close behavior with a custom element. Renders a close icon button by default.

| Prop | Type | Default |
|------|------|---------|
| `className` | `string` | - |
| `asChild` | `boolean` | `false` |

### ActionBarSelectionTrigger

| Prop | Type | Default |
|------|------|---------|
| `count` | `number` | `0` |
| `label` | `string` | - |
| `className` | `string` | - |
| `asChild` | `boolean` | `false` |

Displays "X items selected" when `count` is provided, or custom `label`/`children`.

### ActionBarSeparator

| Prop | Type | Default |
|------|------|---------|
| `className` | `string` | - |
| `asChild` | `boolean` | `false` |
