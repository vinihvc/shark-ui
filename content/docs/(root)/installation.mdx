---
title: Installation
description: Get Shark UI running in your project.
seoDescription: "Install Shark UI in a Tailwind CSS v4 project. Components land in your repository."
---

import { FrameworksList } from "@/components/mdx-components/frameworks-list";

## Prerequisites

Components require [Tailwind CSS v4](https://tailwindcss.com). You need a project with Tailwind already installed.

## Adding components

### CLI

```bash
npx shadcn@latest add @shark/<component>
```

### Manual

Copy component source from the component docs via the Manual tab.

### All at once

```bash
npx shadcn@latest add @shark/ui
```

## New Project Setup

For new projects, you have the `CLI` and `Manual` setup.

### CLI

```bash
npx shadcn@latest init @shark/style
```

### Manual

<FrameworksList />

## Styling

For theming and layout setup, see [Styling](/docs/styling).

## Color system

See [Colors](/docs/colors) for the full palette and variable reference.
