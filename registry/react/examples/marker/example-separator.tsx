import { Marker, MarkerContent } from "@/registry/react/components/marker";

const Example = () => (
  <Marker className="w-full max-w-sm" variant="separator">
    <MarkerContent>Yesterday</MarkerContent>
  </Marker>
);

export default Example;
