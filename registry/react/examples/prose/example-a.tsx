import { Prose } from "@/registry/react/components/prose";

const Example = () => (
  <Prose>
    <p>
      Read more <a href="#about-the-joke-tax">about the joke tax</a> in the
      kingdom archives.
    </p>
  </Prose>
);

export default Example;
