import { CircularSlider } from "@/registry/react/components/circular-slider";

const Example = () => (
  <CircularSlider aria-label="Angle" defaultValue={45} thickness={14} />
);

export default Example;
