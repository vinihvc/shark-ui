import { CircularSlider } from "@/registry/react/components/circular-slider";

const Example = () => (
  <CircularSlider aria-label="Angle" defaultValue={45} size={180} />
);

export default Example;
