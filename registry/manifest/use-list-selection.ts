import type { RegistryItemType } from "@/lib/registry";

const manifest: RegistryItemType = {
  dependencies: ["@ark-ui/react"],
  name: "use-list-selection",
  type: "registry:hook",
};

export default manifest;
