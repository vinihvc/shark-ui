# Implementation Plans

Generated by the improve skill on 2026-08-24 and reconciled on 2026-08-31. Execute **on the current branch (`main`)** in the order below. Do **not** create a new branch, do **not** `git stash`, do **not** switch branches. Each executor: read the plan fully before starting, honor STOP conditions, and update your row when done.

Operator override: work in the existing working tree (may already have unrelated dirty files). Touch only in-scope paths. Do not commit unless the operator asks.

This index was reconciled on 2026-08-31. It inventories every plan file; the historical number `036` is intentionally shared by two independently authored plans and is disambiguated by title.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Bake production URLs into registry JSON | P1 | S | — | DONE |
| 002 | Stop sitemap from advertising 404s and noindex previews | P1 | S | — | DONE |
| 003 | Filter featured blocks on the server for `/blocks` | P1 | S | — | DONE |
| 004 | Fill PWA manifest and footer Projects | P2 | S | — | DONE |
| 005 | Correct Drawer `swipeDirection` docs | P2 | S | — | DONE |
| 006 | Exclusive package-manager command tabs + tiny `pnpm test` | P1 | S | — | DONE |
| 007 | Contain `ComponentSource` reads under `registry/react` | P1 | S | — | DONE |
| 008 | Skip Shiki in `registry:build` composition packaging | P1 | S | — | DONE |
| 009 | Remove dead `_blocks`/`_templates` trees and unused shims | P2 | S | — | DONE |
| 010 | CI lint, Node 26, `pull_request`, honest CONTRIBUTING | P2 | S | 006 (for `pnpm test` step) | DONE |
| 011 | Copied theme CSS matches live preview (`createCssVars` + `.theme-*` dark) | P1 | M | — | DONE |
| 012 | Surgical body theme classes + radius/primary types | P1 | S | — | DONE |
| 013 | Control mobile Gray NativeSelect value | P1 | S | — | DONE |
| 014 | Share ThemeSelector Field+Select (not header sheet) | P2 | M | 013 | DONE |
| 015 | Copy-theme title/width + `/themes` hygiene | P2 | S | 011 | DONE |
| 016 | Keep Snippet compound; drop SelectItem registry | P2 | S | — | DONE |
| 017 | Give every public route intentional metadata and sharpen the homepage promise | P1 | M | — | TODO — re-validate source drift |
| 018 | Add dedicated docs SEO fields and prevent broken internal links | P1 | M | 017 | TODO — re-validate source drift |
| 019 | Make the sitemap deterministic, unique, and limited to canonical pages | P1 | S | 018 | TODO — re-validate source drift |
| 020 | Turn the changelog hub into summaries instead of duplicate full posts | P2 | S | 018 | TODO — re-validate source drift |
| 021 | Stop shipping the desktop showcase to mobile and remove duplicate demo mounts | P2 | M | 017 | TODO — re-validate source drift |
| 022 | Give every published block a stable, indexable detail URL | P2 | M | 017, 019 | TODO — re-validate source drift |
| 023 | Publish the shadcn/Radix migration guide | P1 | S | 018 | TODO — re-validate source drift |
| 024 | Publish the Templates catalog after a release-readiness gate | P2 | M | 017, 019 | BLOCKED — explicit release-readiness confirmation required |
| 025 | Separate search and user agents from AI-training crawlers | P3 | S | — | BLOCKED — first-party bot-policy verification and an operator decision required |
| 026 | Add minimal, factual JSON-LD | P3 | M | 017, 018, 020, 022 | TODO — re-validate source drift |
| 027 | Give DiffContent a bounded flex scrollport | P1 | S | — | TODO — re-validate source drift |
| 028 | Route DiffContent props to one labeled node | P1 | S | 027 | TODO — re-validate source drift |
| 029 | Ship Diff success/destructive CSS variables | P1 | S | — | TODO — re-validate source drift |
| 030 | Use one tint recipe for Diff row and sticky gutter | P2 | S | 027 | TODO — re-validate source drift |
| 031 | Name DiffStats and DiffLine for assistive tech | P2 | S | — | TODO — re-validate source drift |
| 032 | Make result-only tool rows non-interactive | P1 | M | — | BLOCKED — concurrent edits to untracked Tool Result source; re-reconcile |
| 033 | Give Tool Result one status configuration and one customization path | P2 | S | — | BLOCKED — concurrent edits to untracked Tool Result source; re-reconcile |
| 034 | Announce tool-result lifecycle changes and document open-state semantics | P2 | S | 033 | BLOCKED — dependency 033 is blocked by source drift |
| 035 | Preserve high-contrast focus behavior on Tool Result triggers | P3 | S | 032 | BLOCKED — no source changes retained; final verification stopped on source drift |
| 050 | Make the documented Icon Tile size variable effective | P1 | S | — | DONE |
| 051 | Make disabled interactive Icon Tiles visibly unavailable | P2 | S | — | DONE |
| 052 | Protect the Icon Tile public class contract with tests | P2 | M | 050, 051 | DONE — test execution awaits explicit authorization |
| 053 | Complete Icon Tile anatomy and non-SVG documentation | P3 | S | 050 | DONE |
| 057 | Use the card foreground token throughout Card | P1 | S | — | DONE |
| 058 | Restrict Card layout state to direct children | P1 | S | 057 | DONE |
| 059 | Match image media corners to the Card surface | P1 | S | 058 | DONE |
| 060 | Make CardMedia's video contract real | P2 | S | 059 | DONE |
| 061 | Synchronize the Card agent reference with the public API | P2 | S | 060 | DONE |
| 062 | Put CardMedia under the standard Variants section | P3 | S | 060 | DONE |
| 063 | Surface the clickable Card composition pattern | P3 | S | 062 | DONE |
| 036 | State: make documentation copyable in a fresh consumer project | P1 | S | — | DONE |
| 037 | Restore lint-clean State examples | P1 | S | — | DONE |
| 038 | Demonstrate semantic headings in State compositions | P2 | S | 036 | DONE |
| 036 | Questionnaire: quality, accessibility, and publication | P1 | M | — | DONE |
| 041 | Establish a Questionnaire interaction-test baseline | P1 | M | — | DONE — test execution awaits separate authorization |
| 042 | Keep `QuestionnaireChoice` click callbacks rooted on the choice | P1 | S | 041 | DONE — test execution awaits separate authorization |
| 043 | Resolve radio-arrow navigation ownership | P2 | S | 041 | IN PROGRESS |
| 044 | Correct Questionnaire reset-event typing | P3 | S | 041 | TODO |
| 045 | Render Approval Card titles before header actions | P1 | S | — | DONE |
| 046 | Make Approval Card rejection non-submitting by contract | P1 | S | 045 | DONE |
| 047 | Show validation feedback in the default Approval Card example | P2 | S | 045 | DONE |
| 048 | Make the close-button example actually dismiss the Approval Card | P2 | S | 045 | DONE |
| 049 | Restore focused lint cleanliness for Approval Card examples | P3 | S | 047, 048 | DONE |
| 064 | Restore the documented test command and CI gate | P1 | S | — | BLOCKED — test execution awaits explicit authorization |
| 065 | Guarantee AA contrast for FileThumbnail badge labels | P2 | S | 064 | BLOCKED — focused test execution awaits explicit authorization |
| 066 | Reconcile the implementation-plan index | P2 | S | — | DONE |
| 067 | Synchronize the FileThumbnail format catalogue | P2 | S | — | DONE |
| 068 | Correct the FileThumbnail custom-size badge selector | P1 | S | — | DONE |
| 069 | Constrain FileThumbnail format-map tone keys | P2 | S | — | DONE |
| 070 | Document `Rating` as the public component | P1 | S | — | DONE — approved in isolated worktree; awaiting operator integration |
| 071 | Make the Rating icon contract clone-safe | P1 | M | — | BLOCKED — isolated executor lacks the uncommitted Rating test harness and test script |
| 072 | Publish a valid File Upload accepted-types example | P1 | S | — | TODO |
| 073 | Correct QuestionnaireInput anatomy hierarchy | P2 | S | — | TODO |
| 074 | Correct ApprovalCardInput anatomy hierarchy | P2 | S | — | TODO |
| 075 | Remove trailing whitespace from changed MDX | P3 | S | — | TODO |
| 076 | Document Next.js Link via `asChild`, not a source fork | P1 | S | — | DONE |
| 077 | Lock the Link Overlay CSS contract with DOM tests | P1 | S | — | DONE |
| 078 | Make the default Link Overlay demo a heading link on a Card | P2 | S | — | DONE |
| 079 | Link Sidebar docs to the published blocks catalog | P2 | S | — | DONE |
| 080 | Document Shark theme font variables | P3 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 should run first whenever `pnpm registry:build` will run, so later work does not re-bake `localhost` into `public/r`. (Already DONE.)
- 002–005 are independent of each other. (Already DONE.)
- 006–009 are independent of each other. 010 should run after 006 so CI can call `pnpm test`; if 006 is skipped, 010 still lands lint/Node/PR triggers.
- 008 must not switch catalog pages off `highlightedContent` — only `scripts/build-registry.mts`.
- 009 was re-vetted after public `/templates` shipped: still delete private `_blocks`/`_templates` only; add footer Templates; do not touch the live templates page or sitemap.
- 011–015 are the `/themes` audit (2026-08-24). 011 and 012/013 are independent. 014 after 013. 015 after 011 so the dialog still calls the same `createCssVars` name. Do not regenerate all `.bg-*` CSS in 011.
- Theme iframe race is folded into 012 (`ThemesProvider` wipe), not a separate plan.
- 016 is independent of 001–015. Operator wants **compound slots kept**. 016 only removes `SnippetSelectItem` + option register; `items` moves onto `Snippet` (CodeBlock pattern).
- 050 and 051 are independent. Run 052 after both so its class-contract tests describe the final API. Run 053 after 050 because it documents the corrected size-variable API.
- 045–048 are the Approval Card audit batch (2026-08-31). Run 045 first because it fixes the documented title-plus-action composition. Then 046, 047, and 048 may proceed without touching each other's files. Run 049 last so the focused lint check includes the completed example changes.
- 017–026 are the SEO and catalog batch. Plans 024 and 025 require an explicit product/policy decision; do not infer authorization from the presence of source files.
- 027–031 are the Diff batch. Re-read their drift checks before scheduling because the plans were authored against a dirty checkout.
- 041 establishes the DOM-test baseline for the Questionnaire batch. Plans 042–044 remain blocked on that in-progress work.
- 064 restored the test command and CI gate, but its test execution awaits explicit authorization. Plan 065 uses that shared test runner and is likewise awaiting its focused test run; 066 is documentation-only.
- 070 is approved in `/tmp/shark-ui-rating-plans-executor` and awaits operator integration because executor worktrees are isolated. 071 cannot execute in an isolated worktree until its required `test/` harness and `package.json` test script are committed; they exist only as uncommitted changes in the operator worktree. `Rating` is the Shark public name; `RatingGroup` remains only the local Ark UI alias.
- 072–075 are independent documentation fixes audited on 2026-09-01. Execute 072 first if the File Upload usage snippet will be copied by consumers. Plans 073 and 074 deliberately keep the duplicated anatomy corrections in separate files to avoid conflicts in two actively edited docs. Plan 075 is whitespace-only and can run at any time.
- 076–078 are the Link Overlay parity batch (2026-09-01). They do not share files and may run in any order. 076 is docs-only (`asChild` vs source fork, plus `href` optional). 077 adds `test/link-overlay.test.tsx` and is authorized to run that focused test file only; execution also assigned JSDOM `document` on `globalThis` in `test/setup-dom.ts` so React DOM loads on Node 26. 078 rewrites `example-default.tsx` (keep Card, drop inert Buttons). Do not raise `button` in `LinkBox` and do not add Chakra’s text-selection caveat in this batch.
- 079 and 080 are independent MDX plans audited on 2026-09-01. 079 may link only to `/blocks/sidebar`: raw Chart block files are not currently included in the public `BLOCKS` registry. 080 documents the existing `cssVars.theme` font contract and must not change the style manifest.

## Findings considered and rejected

- Static `output: "export"` and disabled `/api/raw` rewrites: documented Hobby tradeoff.
- `_blocks` vs `blocks` route collision: `_` folders are private and do not route. **Dead code in those folders is plan 009**, not a routing bug.
- Three form libraries and duplicated `utils`: intentional registry/product surface.
- Splitting `lib/themes.ts` for readability: low leverage; 011 refactors `createCssVars` merge instead of file-splitting.
- Regenerating entire `styles/themes.css` `.bg-*` from TS in the first batch: HIGH risk; 011 only dark `.theme-*` + snippet completeness.
- Shiki in the copy-theme dialog: deferred (015 explicitly out of scope).
- Converting header customize RadioGroup/Slider to the page Selects: product density, not 014.
- Collapsing duplicated `CardsDemo` calendar columns: layout risk; 015 only logical padding.
- Shipping `highlightedContent` only on the Code tab / deferring Shiki on `/blocks` pages: constrained by static export; deferred. Plan 008 only skips Shiki on the **CLI JSON** path.
- Theme iframe race (`ThemesProvider` vs `postMessage`): re-vetted; root cause is wiping **all** `body` classes (plan 012).
- Chart `ChartStyle` CSS injection: MED confidence; colors are author-controlled (same pattern as typical shadcn charts); docs site uses static configs.
- Aligning `app/robots.ts` with `absoluteUrl`: MED; pinning the production sitemap URL on a single-host static site is reasonable.
- Offline docs “saved” when Serwist is missing: MED; preview path is a UX lie in non-SW environments, not this batch.
- Unifying dual `/view` loaders (flat filesystem vs composition `preview()`): L effort, HIGH risk; legacy chart/sidebar demos must stay (PRODUCT.md).
- Renaming all `Block*`/`Template*` type aliases / merging `lib/blocks.ts`+`lib/templates.ts`: leftover API noise after composition consolidation; low leverage vs 009.
- `pnpm audit` highs (`js-yaml` via `front-matter`, `brace-expansion` via Serwist): trusted changelog MD + build-time glob; pin/overrides later, not 010.
- Direction: public Templates catalog — **already shipping** at `app/(app)/templates/` (nav + sitemap `/templates` + `/view/templates/...`). `_templates` mocks removed in 009. Footer Projects includes Templates.
- CI `pnpm lint:check` was **not** added as a required job: `ultracite check` reports 657 existing diagnostics. CONTRIBUTING still asks contributors to run `lint:fix` locally.
- Direction: grow blocks catalog / LLM `.txt` routes / PWA cache for `/blocks`: product options, not this execution batch.
- Adding husky for `lint-staged`: CONTRIBUTING will stop claiming hooks exist (010); installing a hook framework is optional later.
- Changing `typecheck` from `next build` to `tsc --noEmit`: loses Next plugin types unless a second CI job is designed; deferred.
- Collapsing Snippet to a single non-compound `<Snippet items>` with no slots: operator rejected; 016 keeps `SnippetSelect` / `SnippetCode` / `SnippetCopy`.
- Keeping public `SnippetSelectItem` plus `items` on `SnippetSelect`: unused dual API; 016 drops child registration.
- Tool Result status styling does **not** need `tailwind-variants`: it has one status axis, and a typed configuration record avoids adding a dependency to the copyable registry item.
- Auto-opening Tool Result whenever `status` changes: rejected. `defaultOpen` is intentionally an initial uncontrolled value, consistent with Task and Reasoning; plan 034 documents the controlled path instead.
- Icon Tile registry dependency declarations: verified for Attachment and every migrated published block; no plan needed.
- Icon Tile performance/security work: no async work, I/O, user-data interpreter boundary, or dependency-specific issue exists in this purely visual primitive.
- Card audit (2026-08-31): `group/card` has no current consumer, but removing one harmless class is not worth a standalone plan. No Card security or performance issue was evidenced. A DOM test harness is not proposed for class-only changes; each Card plan uses focused lint, source scans, registry generation where source changes, and whitespace checks.
- State root `border-dashed` without `border`: inherited from the upstream composition and intentional because the documented Outline example opts into a visible border; not worth changing.
- State unit or visual-regression tests: this is a static compositional primitive with preview and type/lint coverage; defer unless it gains behavior or variants with branching logic.
- Questionnaire compound-part discovery through arbitrary wrapper elements: deliberately constrained to direct children and fragments by completed plan 036. Keep that contract documented rather than recursively inspecting arbitrary component trees.
- Link Overlay raising `abbr` like Chakra: leftover, no Shark consumer.
- Link Overlay `asChild` overwriting a child’s `data-slot` (Card → `link-box`): Ark factory behavior; not a stretch bug.
- Link Overlay `LinkBox` raising `button`/`input`: Chakra does not either; Shark `Button` is already `relative`. Product extra, not this batch.
- Link Overlay text-selection caveat (Chakra Caveat section): true of the technique, not a functional bug; omitted from 076 to keep the docs change on the fork/`href` errors.
- MessageBubbleTrigger `type="button"` before `{...rest}`: matches `Button`; overridable by design. Not a form-submit hazard like ApprovalCardReject (046).
- Removing MessageBubbleTrigger's inner span: it is the slide-in animation host; `[&_svg]` would miss non-svg custom children.
- Replacing `ltr:translate-x-2` / `rtl:-translate-x-2` on the trigger icon: this repo has no logical translate utilities; the gradient already uses the same `ltr:` / `rtl:` split.
- `pointer-fine:flex hidden` on the trigger: documented as a desktop hover control; touch uses Context Menu (example-trigger).
- Extracting trigger classes into `tv()`: the part has no variants of its own; ceremony without a consumer.
- Painting MessageBubbleTrigger from `--message-bubble-foreground` (former plan 081): operator rejected after visual regression; trigger colors stay on `group-data-[variant=*]` classes. Do not re-plan.
