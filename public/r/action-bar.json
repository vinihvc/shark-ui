{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "action-bar",
  "type": "registry:ui",
  "dependencies": [
    "@ark-ui/react",
    "lucide-react",
    "tailwind-variants"
  ],
  "registryDependencies": [
    "https://shark.vini.one/r/button.json"
  ],
  "files": [
    {
      "path": "registry/react/components/action-bar.tsx",
      "content": "\"use client\";\n\nimport { ark, Portal } from \"@ark-ui/react\";\nimport { Presence } from \"@ark-ui/react/presence\";\nimport React from \"react\";\nimport { tv, type VariantProps } from \"tailwind-variants\";\nimport { cn } from \"@/lib/utils\";\n\ninterface ActionBarContextValue {\n  isOpen?: boolean;\n  onClose?: () => void;\n  onOpen?: () => void;\n}\n\nconst ActionBarContext = React.createContext({} as ActionBarContextValue);\n\nconst actionBarVariants = tv({\n  slots: {\n    close: [\"shrink-0\"],\n    content: [\n      \"flex w-full max-w-full items-center gap-2\",\n      \"rounded-lg border bg-popover px-4 py-2\",\n      \"text-popover-foreground shadow-lg\",\n    ],\n    positioner: [\"fixed inset-s-0 bottom-0 z-50 flex px-4 pb-4\"],\n    selectionTrigger: [\"shrink-0 font-medium\"],\n    separator: [\"h-4 w-px shrink-0 bg-border\"],\n    trigger: [],\n  },\n  variants: {\n    placement: {\n      bottom: {\n        positioner: \"justify-center\",\n      },\n      \"bottom-end\": {\n        positioner: \"justify-end\",\n      },\n      \"bottom-start\": {\n        positioner: \"justify-start\",\n      },\n    },\n    size: {\n      lg: {\n        content: \"py-2.5 text-base\",\n      },\n      md: {\n        content: \"py-2 text-sm\",\n      },\n      sm: {\n        content: \"py-1.5 text-sm\",\n      },\n    },\n  },\n  defaultVariants: {\n    placement: \"bottom\",\n    size: \"md\",\n  },\n});\n\nexport interface ActionBarProps {\n  /**\n   * The children of the action bar\n   */\n  children: React.ReactNode;\n  /**\n   * The default open state of the action bar\n   */\n  defaultOpen?: boolean;\n  /**\n   * The function to call when the open state of the action bar changes\n   */\n  onOpenChange?: (open: boolean) => void;\n  /**\n   * The open state of the action bar\n   */\n  open?: boolean;\n}\n\nexport const ActionBar = (props: ActionBarProps) => {\n  const { open, defaultOpen = false, onOpenChange, children } = props;\n\n  const [internalOpen, setInternalOpen] = React.useState(defaultOpen);\n\n  const isControlled = open !== undefined;\n  const isOpen = isControlled ? open : internalOpen;\n\n  const handleClose = React.useCallback(() => {\n    if (!isControlled) {\n      setInternalOpen(false);\n    }\n    onOpenChange?.(false);\n  }, [isControlled, onOpenChange]);\n\n  const handleOpen = React.useCallback(() => {\n    if (!isControlled) {\n      setInternalOpen(true);\n    }\n    onOpenChange?.(true);\n  }, [isControlled, onOpenChange]);\n\n  const context = React.useMemo(\n    () => ({ onClose: handleClose, onOpen: handleOpen, isOpen }),\n    [handleClose, handleOpen, isOpen]\n  );\n\n  return (\n    <ActionBarContext.Provider value={context}>\n      {children}\n    </ActionBarContext.Provider>\n  );\n};\n\nexport interface ActionBarTriggerProps\n  extends React.ComponentProps<typeof ark.button> {}\n\nexport const ActionBarTrigger = (props: ActionBarTriggerProps) => {\n  const { className, children, onClick, ...rest } = props;\n\n  const { onOpen, isOpen } = useActionBar();\n  const { trigger } = actionBarVariants();\n\n  const handleClick = (event: React.MouseEvent<HTMLButtonElement>) => {\n    onOpen?.();\n    onClick?.(event);\n  };\n\n  return (\n    <ark.button\n      aria-expanded={isOpen}\n      className={cn(trigger(), className)}\n      data-slot=\"action-bar-trigger\"\n      onClick={handleClick}\n      type=\"button\"\n      {...rest}\n    >\n      {children}\n    </ark.button>\n  );\n};\n\nexport interface ActionBarContentProps\n  extends React.ComponentProps<typeof ark.div>,\n    VariantProps<typeof actionBarVariants> {}\n\nexport const ActionBarContent = (props: ActionBarContentProps) => {\n  const {\n    \"aria-label\": ariaLabel,\n    \"aria-labelledby\": ariaLabelledby,\n    placement = \"bottom\",\n    size = \"md\",\n    className,\n    ...rest\n  } = props;\n  const { isOpen } = useActionBar();\n  const { content, positioner } = actionBarVariants({ placement, size });\n\n  return (\n    <Presence present={isOpen} unmountOnExit>\n      <Portal>\n        <ark.div\n          className={positioner()}\n          data-placement={placement}\n          data-slot=\"action-bar-positioner\"\n        >\n          <ark.div\n            aria-label={\n              ariaLabelledby ? undefined : (ariaLabel ?? \"Selection actions\")\n            }\n            aria-labelledby={ariaLabelledby}\n            className={cn(content(), className)}\n            data-slot=\"action-bar-content\"\n            role=\"toolbar\"\n            {...rest}\n          />\n        </ark.div>\n      </Portal>\n    </Presence>\n  );\n};\n\nexport interface ActionBarCloseProps\n  extends React.ComponentProps<typeof ark.button> {}\n\nexport const ActionBarClose = (props: ActionBarCloseProps) => {\n  const { className, onClick, ...rest } = props;\n\n  const { onClose } = useActionBar();\n  const { close } = actionBarVariants();\n\n  const handleClick = (event: React.MouseEvent<HTMLButtonElement>) => {\n    onClose?.();\n    onClick?.(event);\n  };\n\n  return (\n    <ark.button\n      aria-label=\"Close\"\n      className={cn(close(), className)}\n      data-slot=\"action-bar-close\"\n      onClick={handleClick}\n      type=\"button\"\n      {...rest}\n    />\n  );\n};\n\nexport interface ActionBarSelectionTriggerProps\n  extends React.ComponentProps<typeof ark.span> {\n  /**\n   * The number of items selected\n   */\n  count?: number;\n  /**\n   * The label of the selection trigger\n   */\n  label?: string;\n}\n\nexport const ActionBarSelectionTrigger = (\n  props: ActionBarSelectionTriggerProps\n) => {\n  const { count = 0, label, className, children, ...rest } = props;\n  const { selectionTrigger } = actionBarVariants();\n\n  return (\n    <ark.span\n      className={cn(selectionTrigger(), className)}\n      data-slot=\"action-bar-selection-trigger\"\n      {...rest}\n    >\n      {children ??\n        label ??\n        (count === 1 ? \"1 item selected\" : `${count} items selected`)}\n    </ark.span>\n  );\n};\n\nexport interface ActionBarSeparatorProps\n  extends React.ComponentProps<typeof ark.div> {}\n\nexport const ActionBarSeparator = (props: ActionBarSeparatorProps) => {\n  const { className, ...rest } = props;\n  const { separator } = actionBarVariants();\n\n  return (\n    <ark.div\n      aria-orientation=\"vertical\"\n      className={cn(separator(), className)}\n      data-slot=\"action-bar-separator\"\n      role=\"separator\"\n      {...rest}\n    />\n  );\n};\n\nconst useActionBar = () => {\n  const context = React.useContext(ActionBarContext);\n\n  if (!context) {\n    throw new Error(\"useActionBar must be used within a ActionBarProvider.\");\n  }\n\n  return context;\n};\n",
      "type": "registry:ui"
    }
  ]
}