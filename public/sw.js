var N = [
  {
    revision: null,
    url: "/_next/static/media/cc545e633e20c56d-s.176arc174-8zp.woff2",
  },
  {
    revision: null,
    url: "/_next/static/media/c47649aa31f9e140-s.p.3lxpqujs87tck.woff2",
  },
  {
    revision: null,
    url: "/_next/static/media/a63431c4ac64ea8b-s.386o_ri1vb0yv.woff2",
  },
  {
    revision: null,
    url: "/_next/static/media/91bd2e40df15814e-s.19s5evoqhirey.woff2",
  },
  {
    revision: null,
    url: "/_next/static/media/8b4ed0a90d903ab5-s.44dak0sams9f0.woff2",
  },
  {
    revision: null,
    url: "/_next/static/media/89b21bb081cb7469-s.1fby2rem9ngyr.woff2",
  },
  {
    revision: null,
    url: "/_next/static/media/71b036adf157cdcf-s.0bp8oijd_gu96.woff2",
  },
  {
    revision: null,
    url: "/_next/static/media/70bc3e132a0a741e-s.p.3t6q91iet4nsy.woff2",
  },
  {
    revision: null,
    url: "/_next/static/media/3fe682a82f50d426-s.0vfdmo25voy_0.woff2",
  },
  {
    revision: null,
    url: "/_next/static/media/13bf9871fe164e7f-s.2f7nqdagzwx2-.woff2",
  },
  { revision: null, url: "/_next/static/chunks/turbopack-0y6qirtnnh-3j.js" },
  { revision: null, url: "/_next/static/chunks/42_91sbmlgsry.js" },
  { revision: null, url: "/_next/static/chunks/40sc4sw07ahpa.js" },
  { revision: null, url: "/_next/static/chunks/3qf5vt973wrom.js" },
  { revision: null, url: "/_next/static/chunks/3jgm5iz9gtu1u.js" },
  { revision: null, url: "/_next/static/chunks/3i5eyiwot1ueb.js" },
  { revision: null, url: "/_next/static/chunks/3erg4wvx4l5m3.js" },
  { revision: null, url: "/_next/static/chunks/3cj57r5v0oc1l.js" },
  { revision: null, url: "/_next/static/chunks/3bn0v-1k9f_r8.css" },
  { revision: null, url: "/_next/static/chunks/3bh-ghpo05dqx.js" },
  { revision: null, url: "/_next/static/chunks/38r01orimbwbz.js" },
  { revision: null, url: "/_next/static/chunks/361refofpld47.js" },
  { revision: null, url: "/_next/static/chunks/34kh3o7l3cka_.js" },
  { revision: null, url: "/_next/static/chunks/323aaj98oa0am.js" },
  { revision: null, url: "/_next/static/chunks/30pxkxh3ju2vx.js" },
  { revision: null, url: "/_next/static/chunks/30hgvn6vzud42.js" },
  { revision: null, url: "/_next/static/chunks/2yc4bn8n184ep.css" },
  { revision: null, url: "/_next/static/chunks/2xmyexrgjx8-g.js" },
  { revision: null, url: "/_next/static/chunks/2sdsvoinuupnv.js" },
  { revision: null, url: "/_next/static/chunks/2ppjxhe_ivpb4.js" },
  { revision: null, url: "/_next/static/chunks/2ofilya5c5off.js" },
  { revision: null, url: "/_next/static/chunks/2jz7pb9djrkeb.js" },
  { revision: null, url: "/_next/static/chunks/2j85d88hqnykg.js" },
  { revision: null, url: "/_next/static/chunks/2hodq7teu7wpv.js" },
  { revision: null, url: "/_next/static/chunks/2fpdthhtluua0.js" },
  { revision: null, url: "/_next/static/chunks/2f4vu2e54hnug.js" },
  { revision: null, url: "/_next/static/chunks/2b84veeu3rymh.js" },
  { revision: null, url: "/_next/static/chunks/2ajw0worvfqj-.js" },
  { revision: null, url: "/_next/static/chunks/24r61-_4_6csl.js" },
  { revision: null, url: "/_next/static/chunks/1y6ihu1e1pvgt.js" },
  { revision: null, url: "/_next/static/chunks/1y3qh70r6b-dm.js" },
  { revision: null, url: "/_next/static/chunks/1wpz9nlfg_mt8.js" },
  { revision: null, url: "/_next/static/chunks/1vleox2wscww2.js" },
  { revision: null, url: "/_next/static/chunks/1u82tgks1tn-v.js" },
  { revision: null, url: "/_next/static/chunks/1i6ynzro6ba47.js" },
  { revision: null, url: "/_next/static/chunks/1hvxla0rnvx6x.js" },
  { revision: null, url: "/_next/static/chunks/1dbui9dh0bxn6.js" },
  { revision: null, url: "/_next/static/chunks/1_cmostnfyr_z.js" },
  { revision: null, url: "/_next/static/chunks/162v-1bd9y672.js" },
  { revision: null, url: "/_next/static/chunks/104d5_yeoi8fx.js" },
  { revision: null, url: "/_next/static/chunks/1-ihj67g93pi1.js" },
  { revision: null, url: "/_next/static/chunks/0z39tuyz8ebh7.js" },
  { revision: null, url: "/_next/static/chunks/0vq1btc58h9y_.js" },
  { revision: null, url: "/_next/static/chunks/0ut_2uxf8385x.js" },
  { revision: null, url: "/_next/static/chunks/0two5tsb_xp2c.js" },
  { revision: null, url: "/_next/static/chunks/0t1agfp49ywq2.js" },
  { revision: null, url: "/_next/static/chunks/0p3vq7hhga4v0.js" },
  { revision: null, url: "/_next/static/chunks/0nygh1qioze1p.js" },
  { revision: null, url: "/_next/static/chunks/0ns8v36jg-ogl.js" },
  { revision: null, url: "/_next/static/chunks/0kjrtylgq0w1y.js" },
  { revision: null, url: "/_next/static/chunks/0fhy5uef_crtq.js" },
  { revision: null, url: "/_next/static/chunks/0cz1d0mv5g_q7.js" },
  { revision: null, url: "/_next/static/chunks/0c8anazw313vj.js" },
  { revision: null, url: "/_next/static/chunks/0bo54totm1863.js" },
  { revision: null, url: "/_next/static/chunks/0a_wj91ae_fw_.js" },
  { revision: null, url: "/_next/static/chunks/0a7tqw84129i_.js" },
  { revision: null, url: "/_next/static/chunks/0a0qq0ocdzka3.js" },
  { revision: null, url: "/_next/static/chunks/099wfl3dbb3dc.js" },
  { revision: null, url: "/_next/static/chunks/07w95aw9ukvtf.js" },
  { revision: null, url: "/_next/static/chunks/06mek5wvv4ms7.js" },
  { revision: null, url: "/_next/static/chunks/01qbijdaw-wu-.js" },
  { revision: "126104729e7fcc87c6aa1b9d2ad00ab6b5543097", url: "/" },
  { revision: "126104729e7fcc87c6aa1b9d2ad00ab6b5543097", url: "/~offline" },
];
var f = {
    googleAnalytics: "googleAnalytics",
    precache: "precache-v2",
    prefix: "serwist",
    runtime: "runtime",
    suffix: typeof registration < "u" ? registration.scope : "",
  },
  I = (e) => [f.prefix, e, f.suffix].filter((t) => t && t.length > 0).join("-"),
  Fe = (e) => {
    for (const t of Object.keys(f)) {
      e(t);
    }
  },
  m = {
    getGoogleAnalyticsName: (e) => e || I(f.googleAnalytics),
    getPrecacheName: (e) => e || I(f.precache),
    getPrefix: () => f.prefix,
    getRuntimeName: (e) => e || I(f.runtime),
    getSuffix: () => f.suffix,
    updateDetails: (e) => {
      Fe((t) => {
        const s = e[t];
        typeof s == "string" && (f[t] = s);
      });
    },
  },
  R;
function Y() {
  if (R === void 0) {
    const e = new Response("");
    if ("body" in e) {
      try {
        new Response(e.body), (R = !0);
      } catch {
        R = !1;
      }
    }
    R = !1;
  }
  return R;
}
var je = (e, ...t) => {
  let s = e;
  return t.length > 0 && (s += ` :: ${JSON.stringify(t)}`), s;
};
var Be = je,
  u = class extends Error {
    details;
    constructor(e, t) {
      const s = Be(e, t);
      super(s), (this.name = e), (this.details = t);
    }
  };
var X = (e) =>
  new URL(String(e), location.href).href.replace(
    new RegExp(`^${location.origin}`),
    ""
  );
function x(e) {
  return new Promise((t) => setTimeout(t, e));
}
var L = new Set();
function J(e, t) {
  const s = new URL(e);
  for (const r of t) {
    s.searchParams.delete(r);
  }
  return s.href;
}
async function Z(e, t, s, r) {
  const a = J(t.url, s);
  if (t.url === a) {
    return e.match(t, r);
  }
  const n = { ...r, ignoreSearch: !0 },
    o = await e.keys(t, n);
  for (const i of o) {
    if (a === J(i.url, s)) {
      return e.match(i, r);
    }
  }
}
var ee = class {
    promise;
    resolve;
    reject;
    constructor() {
      this.promise = new Promise((e, t) => {
        (this.resolve = e), (this.reject = t);
      });
    }
  },
  te = async () => {
    for (const e of L) {
      await e();
    }
  },
  Me = "-precache-",
  We = async (e, t = Me) => {
    const s = (await self.caches.keys()).filter(
      (r) => r.includes(t) && r.includes(self.registration.scope) && r !== e
    );
    return await Promise.all(s.map((r) => self.caches.delete(r))), s;
  },
  se = (e) => {
    self.addEventListener("activate", (t) => {
      t.waitUntil(We(m.getPrecacheName(e)).then((s) => {}));
    });
  },
  re = () => {
    self.addEventListener("activate", () => self.clients.claim());
  },
  V = (e, t) => {
    const s = t();
    return e.waitUntil(s), s;
  };
var B = (e, t) => t.some((s) => e instanceof s),
  ae,
  ne;
function Ke() {
  return (
    ae ||
    (ae = [IDBDatabase, IDBObjectStore, IDBIndex, IDBCursor, IDBTransaction])
  );
}
function He() {
  return (
    ne ||
    (ne = [
      IDBCursor.prototype.advance,
      IDBCursor.prototype.continue,
      IDBCursor.prototype.continuePrimaryKey,
    ])
  );
}
var M = new WeakMap(),
  F = new WeakMap(),
  S = new WeakMap();
function Ge(e) {
  const t = new Promise((s, r) => {
    const a = () => {
        e.removeEventListener("success", n), e.removeEventListener("error", o);
      },
      n = () => {
        s(E(e.result)), a();
      },
      o = () => {
        r(e.error), a();
      };
    e.addEventListener("success", n), e.addEventListener("error", o);
  });
  return S.set(t, e), t;
}
function Qe(e) {
  if (M.has(e)) {
    return;
  }
  const t = new Promise((s, r) => {
    const a = () => {
        e.removeEventListener("complete", n),
          e.removeEventListener("error", o),
          e.removeEventListener("abort", o);
      },
      n = () => {
        s(), a();
      },
      o = () => {
        r(e.error || new DOMException("AbortError", "AbortError")), a();
      };
    e.addEventListener("complete", n),
      e.addEventListener("error", o),
      e.addEventListener("abort", o);
  });
  M.set(e, t);
}
var W = {
  get(e, t, s) {
    if (e instanceof IDBTransaction) {
      if (t === "done") {
        return M.get(e);
      }
      if (t === "store") {
        return s.objectStoreNames[1]
          ? void 0
          : s.objectStore(s.objectStoreNames[0]);
      }
    }
    return E(e[t]);
  },
  has(e, t) {
    return e instanceof IDBTransaction && (t === "done" || t === "store")
      ? !0
      : t in e;
  },
  set(e, t, s) {
    return (e[t] = s), !0;
  },
};
function le(e) {
  W = e(W);
}
function ze(e) {
  return He().includes(e)
    ? function (...t) {
        return e.apply(K(this), t), E(this.request);
      }
    : function (...t) {
        return E(e.apply(K(this), t));
      };
}
function Je(e) {
  return typeof e == "function"
    ? ze(e)
    : (e instanceof IDBTransaction && Qe(e), B(e, Ke()) ? new Proxy(e, W) : e);
}
function E(e) {
  if (e instanceof IDBRequest) {
    return Ge(e);
  }
  if (F.has(e)) {
    return F.get(e);
  }
  const t = Je(e);
  return t !== e && (F.set(e, t), S.set(t, e)), t;
}
var K = (e) => S.get(e);
function C(e, t, { blocked: s, upgrade: r, blocking: a, terminated: n } = {}) {
  const o = indexedDB.open(e, t),
    i = E(o);
  return (
    r &&
      o.addEventListener("upgradeneeded", (c) => {
        r(E(o.result), c.oldVersion, c.newVersion, E(o.transaction), c);
      }),
    s && o.addEventListener("blocked", (c) => s(c.oldVersion, c.newVersion, c)),
    i
      .then((c) => {
        n && c.addEventListener("close", () => n()),
          a &&
            c.addEventListener("versionchange", (l) =>
              a(l.oldVersion, l.newVersion, l)
            );
      })
      .catch(() => {}),
    i
  );
}
function ue(e, { blocked: t } = {}) {
  const s = indexedDB.deleteDatabase(e);
  return (
    t && s.addEventListener("blocked", (r) => t(r.oldVersion, r)),
    E(s).then(() => {})
  );
}
var Ye = ["get", "getKey", "getAll", "getAllKeys", "count"],
  Xe = ["put", "add", "delete", "clear"],
  j = new Map();
function oe(e, t) {
  if (!(e instanceof IDBDatabase && !(t in e) && typeof t == "string")) {
    return;
  }
  if (j.get(t)) {
    return j.get(t);
  }
  const s = t.replace(/FromIndex$/, ""),
    r = t !== s,
    a = Xe.includes(s);
  if (
    !(s in (r ? IDBIndex : IDBObjectStore).prototype && (a || Ye.includes(s)))
  ) {
    return;
  }
  const n = async function (o, ...i) {
    let c = this.transaction(o, a ? "readwrite" : "readonly"),
      l = c.store;
    return (
      r && (l = l.index(i.shift())),
      (await Promise.all([l[s](...i), a && c.done]))[0]
    );
  };
  return j.set(t, n), n;
}
le((e) => ({
  ...e,
  get: (t, s, r) => oe(t, s) || e.get(t, s, r),
  has: (t, s) => !!oe(t, s) || e.has(t, s),
}));
var Ze = ["continue", "continuePrimaryKey", "advance"],
  ie = {},
  H = new WeakMap(),
  he = new WeakMap(),
  et = {
    get(e, t) {
      if (!Ze.includes(t)) {
        return e[t];
      }
      let s = ie[t];
      return (
        s ||
          (s = ie[t] =
            function (...r) {
              H.set(this, he.get(this)[t](...r));
            }),
        s
      );
    },
  };
async function* tt(...e) {
  let t = this;
  if ((t instanceof IDBCursor || (t = await t.openCursor(...e)), !t)) {
    return;
  }
  t = t;
  const s = new Proxy(t, et);
  for (he.set(s, t), S.set(s, K(t)); t; ) {
    yield s, (t = await (H.get(s) || t.continue())), H.delete(s);
  }
}
function ce(e, t) {
  return (
    (t === Symbol.asyncIterator &&
      B(e, [IDBIndex, IDBObjectStore, IDBCursor])) ||
    (t === "iterate" && B(e, [IDBIndex, IDBObjectStore]))
  );
}
le((e) => ({
  ...e,
  get(t, s, r) {
    return ce(t, s) ? tt : e.get(t, s, r);
  },
  has(t, s) {
    return ce(t, s) || e.has(t, s);
  },
}));
var fe = async (e, t) => {
    let s = null;
    if ((e.url && (s = new URL(e.url).origin), s !== self.location.origin)) {
      throw new u("cross-origin-copy-response", { origin: s });
    }
    const r = e.clone(),
      a = {
        headers: new Headers(r.headers),
        status: r.status,
        statusText: r.statusText,
      },
      n = t ? t(a) : a,
      o = Y() ? r.body : await r.blob();
    return new Response(o, n);
  },
  me = () => {
    self.__WB_DISABLE_DEV_LOGS = !0;
  },
  de = 3,
  st = "serwist-background-sync",
  w = "requests",
  D = "queueName",
  rt = class {
    _db = null;
    async addEntry(e) {
      const t = (await this.getDb()).transaction(w, "readwrite", {
        durability: "relaxed",
      });
      await t.store.add(e), await t.done;
    }
    async getFirstEntryId() {
      return (await (await this.getDb()).transaction(w).store.openCursor())
        ?.value.id;
    }
    async getAllEntriesByQueueName(e) {
      const t = await (await this.getDb()).getAllFromIndex(
        w,
        D,
        IDBKeyRange.only(e)
      );
      return t || [];
    }
    async getEntryCountByQueueName(e) {
      return (await this.getDb()).countFromIndex(w, D, IDBKeyRange.only(e));
    }
    async deleteEntry(e) {
      await (await this.getDb()).delete(w, e);
    }
    async getFirstEntryByQueueName(e) {
      return await this.getEndEntryFromIndex(IDBKeyRange.only(e), "next");
    }
    async getLastEntryByQueueName(e) {
      return await this.getEndEntryFromIndex(IDBKeyRange.only(e), "prev");
    }
    async getEndEntryFromIndex(e, t) {
      return (
        await (
          await this.getDb()
        )
          .transaction(w)
          .store.index(D)
          .openCursor(e, t)
      )?.value;
    }
    async getDb() {
      return (
        this._db || (this._db = await C(st, de, { upgrade: this._upgradeDb })),
        this._db
      );
    }
    _upgradeDb(e, t) {
      t > 0 &&
        t < de &&
        e.objectStoreNames.contains(w) &&
        e.deleteObjectStore(w),
        e
          .createObjectStore(w, { autoIncrement: !0, keyPath: "id" })
          .createIndex(D, D, { unique: !1 });
    }
  },
  we = class {
    _queueName;
    _queueDb;
    constructor(e) {
      (this._queueName = e), (this._queueDb = new rt());
    }
    async pushEntry(e) {
      delete e.id,
        (e.queueName = this._queueName),
        await this._queueDb.addEntry(e);
    }
    async unshiftEntry(e) {
      const t = await this._queueDb.getFirstEntryId();
      t ? (e.id = t - 1) : delete e.id,
        (e.queueName = this._queueName),
        await this._queueDb.addEntry(e);
    }
    async popEntry() {
      return this._removeEntry(
        await this._queueDb.getLastEntryByQueueName(this._queueName)
      );
    }
    async shiftEntry() {
      return this._removeEntry(
        await this._queueDb.getFirstEntryByQueueName(this._queueName)
      );
    }
    async getAll() {
      return await this._queueDb.getAllEntriesByQueueName(this._queueName);
    }
    async size() {
      return await this._queueDb.getEntryCountByQueueName(this._queueName);
    }
    async deleteEntry(e) {
      await this._queueDb.deleteEntry(e);
    }
    async _removeEntry(e) {
      return e && (await this.deleteEntry(e.id)), e;
    }
  },
  at = [
    "method",
    "referrer",
    "referrerPolicy",
    "mode",
    "credentials",
    "cache",
    "redirect",
    "integrity",
    "keepalive",
  ],
  z = class Q {
    _requestData;
    static async fromRequest(t) {
      const s = { headers: {}, url: t.url };
      t.method !== "GET" && (s.body = await t.clone().arrayBuffer()),
        t.headers.forEach((r, a) => {
          s.headers[a] = r;
        });
      for (const r of at) {
        t[r] !== void 0 && (s[r] = t[r]);
      }
      return new Q(s);
    }
    constructor(t) {
      t.mode === "navigate" && (t.mode = "same-origin"),
        (this._requestData = t);
    }
    toObject() {
      const t = { ...this._requestData };
      return (
        (t.headers = { ...this._requestData.headers }),
        t.body && (t.body = t.body.slice(0)),
        t
      );
    }
    toRequest() {
      return new Request(this._requestData.url, this._requestData);
    }
    clone() {
      return new Q(this.toObject());
    }
  },
  pe = "serwist-background-sync",
  nt = 1440 * 7,
  G = new Set(),
  ge = (e) => {
    const t = {
      request: new z(e.requestData).toRequest(),
      timestamp: e.timestamp,
    };
    return e.metadata && (t.metadata = e.metadata), t;
  },
  ye = class {
    _name;
    _onSync;
    _maxRetentionTime;
    _queueStore;
    _forceSyncFallback;
    _syncInProgress = !1;
    _requestsAddedDuringSync = !1;
    constructor(
      e,
      { forceSyncFallback: t, onSync: s, maxRetentionTime: r } = {}
    ) {
      if (G.has(e)) {
        throw new u("duplicate-queue-name", { name: e });
      }
      G.add(e),
        (this._name = e),
        (this._onSync = s || this.replayRequests),
        (this._maxRetentionTime = r || nt),
        (this._forceSyncFallback = !!t),
        (this._queueStore = new we(this._name)),
        this._addSyncListener();
    }
    get name() {
      return this._name;
    }
    async pushRequest(e) {
      await this._addRequest(e, "push");
    }
    async unshiftRequest(e) {
      await this._addRequest(e, "unshift");
    }
    async popRequest() {
      return this._removeRequest("pop");
    }
    async shiftRequest() {
      return this._removeRequest("shift");
    }
    async getAll() {
      const e = await this._queueStore.getAll(),
        t = Date.now(),
        s = [];
      for (const r of e) {
        const a = this._maxRetentionTime * 60 * 1e3;
        t - r.timestamp > a
          ? await this._queueStore.deleteEntry(r.id)
          : s.push(ge(r));
      }
      return s;
    }
    async size() {
      return await this._queueStore.size();
    }
    async _addRequest(
      { request: e, metadata: t, timestamp: s = Date.now() },
      r
    ) {
      const a = {
        requestData: (await z.fromRequest(e.clone())).toObject(),
        timestamp: s,
      };
      switch ((t && (a.metadata = t), r)) {
        case "push":
          await this._queueStore.pushEntry(a);
          break;
        case "unshift":
          await this._queueStore.unshiftEntry(a);
          break;
      }
      this._syncInProgress
        ? (this._requestsAddedDuringSync = !0)
        : await this.registerSync();
    }
    async _removeRequest(e) {
      let t = Date.now(),
        s;
      switch (e) {
        case "pop":
          s = await this._queueStore.popEntry();
          break;
        case "shift":
          s = await this._queueStore.shiftEntry();
          break;
      }
      if (s) {
        const r = this._maxRetentionTime * 60 * 1e3;
        return t - s.timestamp > r ? this._removeRequest(e) : ge(s);
      }
    }
    async replayRequests() {
      let e;
      while ((e = await this.shiftRequest())) {
        try {
          await fetch(e.request.clone());
        } catch {
          throw (
            (await this.unshiftRequest(e),
            new u("queue-replay-failed", { name: this._name }))
          );
        }
      }
    }
    async registerSync() {
      if ("sync" in self.registration && !this._forceSyncFallback) {
        try {
          await self.registration.sync.register(`${pe}:${this._name}`);
        } catch {}
      }
    }
    _addSyncListener() {
      "sync" in self.registration && !this._forceSyncFallback
        ? self.addEventListener("sync", (e) => {
            if (e.tag === `${pe}:${this._name}`) {
              const t = async () => {
                this._syncInProgress = !0;
                let s;
                try {
                  await this._onSync({ queue: this });
                } catch (r) {
                  if (r instanceof Error) {
                    throw ((s = r), s);
                  }
                } finally {
                  this._requestsAddedDuringSync &&
                    !(s && !e.lastChance) &&
                    (await this.registerSync()),
                    (this._syncInProgress = !1),
                    (this._requestsAddedDuringSync = !1);
                }
              };
              e.waitUntil(t());
            }
          })
        : this._onSync({ queue: this });
    }
    static get _queueNames() {
      return G;
    }
  },
  Ne = class {
    _queue;
    constructor(e, t) {
      this._queue = new ye(e, t);
    }
    async fetchDidFail({ request: e }) {
      await this._queue.pushRequest({ request: e });
    }
  },
  Ee = {
    cacheWillUpdate: async ({ response: e }) =>
      e.status === 200 || e.status === 0 ? e : null,
  };
function T(e) {
  return typeof e == "string" ? new Request(e) : e;
}
var _e = class {
    event;
    request;
    url;
    params;
    _cacheKeys = {};
    _strategy;
    _handlerDeferred;
    _extendLifetimePromises;
    _plugins;
    _pluginStateMap;
    constructor(e, t) {
      (this.event = t.event),
        (this.request = t.request),
        t.url && ((this.url = t.url), (this.params = t.params)),
        (this._strategy = e),
        (this._handlerDeferred = new ee()),
        (this._extendLifetimePromises = []),
        (this._plugins = [...e.plugins]),
        (this._pluginStateMap = new Map());
      for (const s of this._plugins) {
        this._pluginStateMap.set(s, {});
      }
      this.event.waitUntil(this._handlerDeferred.promise);
    }
    async fetch(e) {
      let { event: t } = this,
        s = T(e),
        r = await this.getPreloadResponse();
      if (r) {
        return r;
      }
      const a = this.hasCallback("fetchDidFail") ? s.clone() : null;
      try {
        for (const o of this.iterateCallbacks("requestWillFetch")) {
          s = await o({ event: t, request: s.clone() });
        }
      } catch (o) {
        if (o instanceof Error) {
          throw new u("plugin-error-request-will-fetch", {
            thrownErrorMessage: o.message,
          });
        }
      }
      const n = s.clone();
      try {
        let o;
        o = await fetch(
          s,
          s.mode === "navigate" ? void 0 : this._strategy.fetchOptions
        );
        for (const i of this.iterateCallbacks("fetchDidSucceed")) {
          o = await i({ event: t, request: n, response: o });
        }
        return o;
      } catch (o) {
        throw (
          (a &&
            (await this.runCallbacks("fetchDidFail", {
              error: o,
              event: t,
              originalRequest: a.clone(),
              request: n.clone(),
            })),
          o)
        );
      }
    }
    async fetchAndCachePut(e) {
      const t = await this.fetch(e),
        s = t.clone();
      return this.waitUntil(this.cachePut(e, s)), t;
    }
    async cacheMatch(e) {
      let t = T(e),
        s,
        { cacheName: r, matchOptions: a } = this._strategy,
        n = await this.getCacheKey(t, "read"),
        o = { ...a, cacheName: r };
      s = await caches.match(n, o);
      for (const i of this.iterateCallbacks("cachedResponseWillBeUsed")) {
        s =
          (await i({
            cachedResponse: s,
            cacheName: r,
            event: this.event,
            matchOptions: a,
            request: n,
          })) || void 0;
      }
      return s;
    }
    async cachePut(e, t) {
      const s = T(e);
      await x(0);
      const r = await this.getCacheKey(s, "write");
      if (!t) {
        throw new u("cache-put-with-no-response", { url: X(r.url) });
      }
      const a = await this._ensureResponseSafeToCache(t);
      if (!a) {
        return !1;
      }
      const { cacheName: n, matchOptions: o } = this._strategy,
        i = await self.caches.open(n),
        c = this.hasCallback("cacheDidUpdate"),
        l = c ? await Z(i, r.clone(), ["__WB_REVISION__"], o) : null;
      try {
        await i.put(r, c ? a.clone() : a);
      } catch (h) {
        if (h instanceof Error) {
          throw (h.name === "QuotaExceededError" && (await te()), h);
        }
      }
      for (const h of this.iterateCallbacks("cacheDidUpdate")) {
        await h({
          cacheName: n,
          event: this.event,
          newResponse: a.clone(),
          oldResponse: l,
          request: r,
        });
      }
      return !0;
    }
    async getCacheKey(e, t) {
      const s = `${e.url} | ${t}`;
      if (!this._cacheKeys[s]) {
        let r = e;
        for (const a of this.iterateCallbacks("cacheKeyWillBeUsed")) {
          r = T(
            await a({
              event: this.event,
              mode: t,
              params: this.params,
              request: r,
            })
          );
        }
        this._cacheKeys[s] = r;
      }
      return this._cacheKeys[s];
    }
    hasCallback(e) {
      for (const t of this._strategy.plugins) {
        if (e in t) {
          return !0;
        }
      }
      return !1;
    }
    async runCallbacks(e, t) {
      for (const s of this.iterateCallbacks(e)) {
        await s(t);
      }
    }
    *iterateCallbacks(e) {
      for (const t of this._strategy.plugins) {
        if (typeof t[e] == "function") {
          const s = this._pluginStateMap.get(t);
          yield (a) => {
            const n = { ...a, state: s };
            return t[e](n);
          };
        }
      }
    }
    waitUntil(e) {
      return this._extendLifetimePromises.push(e), e;
    }
    async doneWaiting() {
      let e;
      while ((e = this._extendLifetimePromises.shift())) {
        await e;
      }
    }
    destroy() {
      this._handlerDeferred.resolve(null);
    }
    async getPreloadResponse() {
      if (
        this.event instanceof FetchEvent &&
        this.event.request.mode === "navigate" &&
        "preloadResponse" in this.event
      ) {
        try {
          const e = await this.event.preloadResponse;
          if (e) {
            return e;
          }
        } catch {}
      }
    }
    async _ensureResponseSafeToCache(e) {
      let t = e,
        s = !1;
      for (const r of this.iterateCallbacks("cacheWillUpdate")) {
        if (
          ((t =
            (await r({
              event: this.event,
              request: this.request,
              response: t,
            })) || void 0),
          (s = !0),
          !t)
        ) {
          break;
        }
      }
      return s || (t && t.status !== 200 && (t = void 0)), t;
    }
  },
  b = class {
    cacheName;
    plugins;
    fetchOptions;
    matchOptions;
    constructor(e = {}) {
      (this.cacheName = m.getRuntimeName(e.cacheName)),
        (this.plugins = e.plugins || []),
        (this.fetchOptions = e.fetchOptions),
        (this.matchOptions = e.matchOptions);
    }
    handle(e) {
      const [t] = this.handleAll(e);
      return t;
    }
    handleAll(e) {
      e instanceof FetchEvent && (e = { event: e, request: e.request });
      const t = e.event,
        s = typeof e.request == "string" ? new Request(e.request) : e.request,
        r = new _e(
          this,
          e.url
            ? { event: t, params: e.params, request: s, url: e.url }
            : { event: t, request: s }
        ),
        a = this._getResponse(r, s, t);
      return [a, this._awaitComplete(a, r, s, t)];
    }
    async _getResponse(e, t, s) {
      await e.runCallbacks("handlerWillStart", { event: s, request: t });
      let r;
      try {
        if (
          ((r = await this._handle(t, e)), r === void 0 || r.type === "error")
        ) {
          throw new u("no-response", { url: t.url });
        }
      } catch (a) {
        if (a instanceof Error) {
          for (const n of e.iterateCallbacks("handlerDidError")) {
            if (
              ((r = await n({ error: a, event: s, request: t })), r !== void 0)
            ) {
              break;
            }
          }
        }
        if (!r) {
          throw a;
        }
      }
      for (const a of e.iterateCallbacks("handlerWillRespond")) {
        r = await a({ event: s, request: t, response: r });
      }
      return r;
    }
    async _awaitComplete(e, t, s, r) {
      let a, n;
      try {
        a = await e;
      } catch {}
      try {
        await t.runCallbacks("handlerDidRespond", {
          event: r,
          request: s,
          response: a,
        }),
          await t.doneWaiting();
      } catch (o) {
        o instanceof Error && (n = o);
      }
      if (
        (await t.runCallbacks("handlerDidComplete", {
          error: n,
          event: r,
          request: s,
          response: a,
        }),
        t.destroy(),
        n)
      ) {
        throw n;
      }
    }
  };
var _ = class extends b {
    _networkTimeoutSeconds;
    constructor(e = {}) {
      super(e),
        this.plugins.some((t) => "cacheWillUpdate" in t) ||
          this.plugins.unshift(Ee),
        (this._networkTimeoutSeconds = e.networkTimeoutSeconds || 0);
    }
    async _handle(e, t) {
      let s = [],
        r = [],
        a;
      if (this._networkTimeoutSeconds) {
        const { id: i, promise: c } = this._getTimeoutPromise({
          handler: t,
          logs: s,
          request: e,
        });
        (a = i), r.push(c);
      }
      const n = this._getNetworkPromise({
        handler: t,
        logs: s,
        request: e,
        timeoutId: a,
      });
      r.push(n);
      const o = await t.waitUntil(
        (async () => (await t.waitUntil(Promise.race(r))) || (await n))()
      );
      if (!o) {
        throw new u("no-response", { url: e.url });
      }
      return o;
    }
    _getTimeoutPromise({ request: e, logs: t, handler: s }) {
      let r;
      return {
        id: r,
        promise: new Promise((a) => {
          r = setTimeout(async () => {
            a(await s.cacheMatch(e));
          }, this._networkTimeoutSeconds * 1e3);
        }),
      };
    }
    async _getNetworkPromise({
      timeoutId: e,
      request: t,
      logs: s,
      handler: r,
    }) {
      let a, n;
      try {
        n = await r.fetchAndCachePut(t);
      } catch (o) {
        o instanceof Error && (a = o);
      }
      return e && clearTimeout(e), (a || !n) && (n = await r.cacheMatch(t)), n;
    }
  },
  O = class extends b {
    _networkTimeoutSeconds;
    constructor(e = {}) {
      super(e), (this._networkTimeoutSeconds = e.networkTimeoutSeconds || 0);
    }
    async _handle(e, t) {
      let s, r;
      try {
        const a = [t.fetch(e)];
        if (this._networkTimeoutSeconds) {
          const n = x(this._networkTimeoutSeconds * 1e3);
          a.push(n);
        }
        if (((r = await Promise.race(a)), !r)) {
          throw new Error(
            `Timed out the network response after ${this._networkTimeoutSeconds} seconds.`
          );
        }
      } catch (a) {
        a instanceof Error && (s = a);
      }
      if (!r) {
        throw new u("no-response", { error: s, url: e.url });
      }
      return r;
    }
  };
var q = (e) => (e && typeof e == "object" ? e : { handle: e }),
  d = class {
    handler;
    match;
    method;
    catchHandler;
    constructor(e, t, s = "GET") {
      (this.handler = q(t)), (this.match = e), (this.method = s);
    }
    setCatchHandler(e) {
      this.catchHandler = q(e);
    }
  },
  ve = class k extends b {
    _fallbackToNetwork;
    static defaultPrecacheCacheabilityPlugin = {
      async cacheWillUpdate({ response: t }) {
        return !t || t.status >= 400 ? null : t;
      },
    };
    static copyRedirectedCacheableResponsesPlugin = {
      async cacheWillUpdate({ response: t }) {
        return t.redirected ? await fe(t) : t;
      },
    };
    constructor(t = {}) {
      (t.cacheName = m.getPrecacheName(t.cacheName)),
        super(t),
        (this._fallbackToNetwork = t.fallbackToNetwork !== !1),
        this.plugins.push(k.copyRedirectedCacheableResponsesPlugin);
    }
    async _handle(t, s) {
      const r = await s.getPreloadResponse();
      if (r) {
        return r;
      }
      const a = await s.cacheMatch(t);
      return (
        a ||
        (s.event && s.event.type === "install"
          ? await this._handleInstall(t, s)
          : await this._handleFetch(t, s))
      );
    }
    async _handleFetch(t, s) {
      let r,
        a = s.params || {};
      if (this._fallbackToNetwork) {
        const n = a.integrity,
          o = t.integrity,
          i = !o || o === n;
        if (
          ((r = await s.fetch(
            new Request(t, {
              integrity: t.mode === "no-cors" ? void 0 : o || n,
            })
          )),
          n && i && t.mode !== "no-cors")
        ) {
          this._useDefaultCacheabilityPluginIfNeeded();
          const c = await s.cachePut(t, r.clone());
        }
      } else {
        throw new u("missing-precache-entry", {
          cacheName: this.cacheName,
          url: t.url,
        });
      }
      return r;
    }
    async _handleInstall(t, s) {
      this._useDefaultCacheabilityPluginIfNeeded();
      const r = await s.fetch(t);
      if (!(await s.cachePut(t, r.clone()))) {
        throw new u("bad-precaching-response", {
          status: r.status,
          url: t.url,
        });
      }
      return r;
    }
    _useDefaultCacheabilityPluginIfNeeded() {
      let t = null,
        s = 0;
      for (const [r, a] of this.plugins.entries()) {
        a !== k.copyRedirectedCacheableResponsesPlugin &&
          (a === k.defaultPrecacheCacheabilityPlugin && (t = r),
          a.cacheWillUpdate && s++);
      }
      s === 0
        ? this.plugins.push(k.defaultPrecacheCacheabilityPlugin)
        : s > 1 && t !== null && this.plugins.splice(t, 1);
    }
  },
  be = class extends d {
    _allowlist;
    _denylist;
    constructor(e, { allowlist: t = [/./], denylist: s = [] } = {}) {
      super((r) => this._match(r), e),
        (this._allowlist = t),
        (this._denylist = s);
    }
    _match({ url: e, request: t }) {
      if (t && t.mode !== "navigate") {
        return !1;
      }
      const s = e.pathname + e.search;
      for (const r of this._denylist) {
        if (r.test(s)) {
          return !1;
        }
      }
      return !!this._allowlist.some((r) => r.test(s));
    }
  },
  Re = () => !!self.registration?.navigationPreload,
  xe = (e) => {
    Re() &&
      self.addEventListener("activate", (t) => {
        t.waitUntil(
          self.registration.navigationPreload.enable().then(() => {
            e && self.registration.navigationPreload.setHeaderValue(e);
          })
        );
      });
  };
var ot = (e, t = []) => {
  for (const s of [...e.searchParams.keys()]) {
    t.some((r) => r.test(s)) && e.searchParams.delete(s);
  }
  return e;
};
function* De(
  e,
  {
    directoryIndex: t = "index.html",
    ignoreURLParametersMatching: s = [/^utm_/, /^fbclid$/],
    cleanURLs: r = !0,
    urlManipulation: a,
  } = {}
) {
  const n = new URL(e, location.href);
  (n.hash = ""), yield n.href;
  const o = ot(n, s);
  if ((yield o.href, t && o.pathname.endsWith("/"))) {
    const i = new URL(o.href);
    (i.pathname += t), yield i.href;
  }
  if (r) {
    const i = new URL(o.href);
    (i.pathname += ".html"), yield i.href;
  }
  if (a) {
    const i = a({ url: n });
    for (const c of i) {
      yield c.href;
    }
  }
}
var ke = class extends d {
    constructor(e, t, s) {
      const r = ({ url: a }) => {
        const n = e.exec(a.href);
        if (n && !(a.origin !== location.origin && n.index !== 0)) {
          return n.slice(1);
        }
      };
      super(r, t, s);
    }
  },
  qe = (e) => {
    m.updateDetails(e);
  },
  it = "__WB_REVISION__",
  Se = (e) => {
    if (!e) {
      throw new u("add-to-cache-list-unexpected-type", { entry: e });
    }
    if (typeof e == "string") {
      const n = new URL(e, location.href);
      return { cacheKey: n.href, url: n.href };
    }
    const { revision: t, url: s } = e;
    if (!s) {
      throw new u("add-to-cache-list-unexpected-type", { entry: e });
    }
    if (!t) {
      const n = new URL(s, location.href);
      return { cacheKey: n.href, url: n.href };
    }
    const r = new URL(s, location.href),
      a = new URL(s, location.href);
    return r.searchParams.set(it, t), { cacheKey: r.href, url: a.href };
  },
  Ce = class {
    updatedURLs = [];
    notUpdatedURLs = [];
    handlerWillStart = async ({ request: e, state: t }) => {
      t && (t.originalRequest = e);
    };
    cachedResponseWillBeUsed = async ({
      event: e,
      state: t,
      cachedResponse: s,
    }) => {
      if (
        e.type === "install" &&
        t?.originalRequest &&
        t.originalRequest instanceof Request
      ) {
        const r = t.originalRequest.url;
        s ? this.notUpdatedURLs.push(r) : this.updatedURLs.push(r);
      }
      return s;
    };
  },
  Te = (e, t, s) => {
    if (typeof e == "string") {
      const r = new URL(e, location.href),
        a = ({ url: n }) => n.href === r.href;
      return new d(a, t, s);
    }
    if (e instanceof RegExp) {
      return new ke(e, t, s);
    }
    if (typeof e == "function") {
      return new d(e, t, s);
    }
    if (e instanceof d) {
      return e;
    }
    throw new u("unsupported-route-type", {
      funcName: "parseRoute",
      moduleName: "serwist",
      paramName: "capture",
    });
  };
var Oe = async (e, t, s) => {
  const r = t.map((o, i) => ({ index: i, item: o })),
    a = async (o) => {
      const i = [];
      for (;;) {
        const c = r.pop();
        if (!c) {
          return o(i);
        }
        const l = await s(c.item);
        i.push({ index: c.index, result: l });
      }
    },
    n = Array.from({ length: e }, () => new Promise(a));
  return (await Promise.all(n))
    .flat()
    .sort((o, i) => (o.index < i.index ? -1 : 1))
    .map((o) => o.result);
};
var fs =
  typeof navigator < "u" &&
  /^((?!chrome|android).)*safari/i.test(navigator.userAgent);
var ct = "serwist-expiration",
  A = "cache-entries",
  Ae = (e) => {
    const t = new URL(e, location.href);
    return (t.hash = ""), t.href;
  },
  lt = class {
    _cacheName;
    _db = null;
    constructor(e) {
      this._cacheName = e;
    }
    _getId(e) {
      return `${this._cacheName}|${Ae(e)}`;
    }
    _upgradeDb(e) {
      const t = e.createObjectStore(A, { keyPath: "id" });
      t.createIndex("cacheName", "cacheName", { unique: !1 }),
        t.createIndex("timestamp", "timestamp", { unique: !1 });
    }
    _upgradeDbAndDeleteOldDbs(e) {
      this._upgradeDb(e), this._cacheName && ue(this._cacheName);
    }
    async setTimestamp(e, t) {
      e = Ae(e);
      const s = {
          cacheName: this._cacheName,
          id: this._getId(e),
          timestamp: t,
          url: e,
        },
        r = (await this.getDb()).transaction(A, "readwrite", {
          durability: "relaxed",
        });
      await r.store.put(s), await r.done;
    }
    async getTimestamp(e) {
      return (await (await this.getDb()).get(A, this._getId(e)))?.timestamp;
    }
    async expireEntries(e, t) {
      let s = await (await this.getDb())
          .transaction(A, "readwrite")
          .store.index("timestamp")
          .openCursor(null, "prev"),
        r = [],
        a = 0;
      while (s) {
        const n = s.value;
        n.cacheName === this._cacheName &&
          ((e && n.timestamp < e) || (t && a >= t)
            ? (s.delete(), r.push(n.url))
            : a++),
          (s = await s.continue());
      }
      return r;
    }
    async getDb() {
      return (
        this._db ||
          (this._db = await C(ct, 1, {
            upgrade: this._upgradeDbAndDeleteOldDbs.bind(this),
          })),
        this._db
      );
    }
  },
  ut = class {
    _isRunning = !1;
    _rerunRequested = !1;
    _maxEntries;
    _maxAgeSeconds;
    _matchOptions;
    _cacheName;
    _timestampModel;
    constructor(e, t = {}) {
      (this._maxEntries = t.maxEntries),
        (this._maxAgeSeconds = t.maxAgeSeconds),
        (this._matchOptions = t.matchOptions),
        (this._cacheName = e),
        (this._timestampModel = new lt(e));
    }
    async expireEntries() {
      if (this._isRunning) {
        this._rerunRequested = !0;
        return;
      }
      this._isRunning = !0;
      const e = this._maxAgeSeconds
          ? Date.now() - this._maxAgeSeconds * 1e3
          : 0,
        t = await this._timestampModel.expireEntries(e, this._maxEntries),
        s = await self.caches.open(this._cacheName);
      for (const r of t) {
        await s.delete(r, this._matchOptions);
      }
      (this._isRunning = !1),
        this._rerunRequested &&
          ((this._rerunRequested = !1), this.expireEntries());
    }
    async updateTimestamp(e) {
      await this._timestampModel.setTimestamp(e, Date.now());
    }
    async isURLExpired(e) {
      if (!this._maxAgeSeconds) {
        return !1;
      }
      const t = await this._timestampModel.getTimestamp(e),
        s = Date.now() - this._maxAgeSeconds * 1e3;
      return t === void 0 ? !0 : t < s;
    }
    async delete() {
      (this._rerunRequested = !1),
        await this._timestampModel.expireEntries(Number.POSITIVE_INFINITY);
    }
  },
  ht = (e) => {
    L.add(e);
  },
  Ue = class {
    _config;
    _cacheExpirations;
    constructor(e = {}) {
      (this._config = e),
        (this._cacheExpirations = new Map()),
        this._config.maxAgeFrom || (this._config.maxAgeFrom = "last-fetched"),
        this._config.purgeOnQuotaError &&
          ht(() => this.deleteCacheAndMetadata());
    }
    _getCacheExpiration(e) {
      if (e === m.getRuntimeName()) {
        throw new u("expire-custom-caches-only");
      }
      let t = this._cacheExpirations.get(e);
      return (
        t || ((t = new ut(e, this._config)), this._cacheExpirations.set(e, t)),
        t
      );
    }
    cachedResponseWillBeUsed({
      event: e,
      cacheName: t,
      request: s,
      cachedResponse: r,
    }) {
      if (!r) {
        return null;
      }
      const a = this._isResponseDateFresh(r),
        n = this._getCacheExpiration(t),
        o = this._config.maxAgeFrom === "last-used",
        i = (async () => {
          o && (await n.updateTimestamp(s.url)), await n.expireEntries();
        })();
      try {
        e.waitUntil(i);
      } catch {}
      return a ? r : null;
    }
    _isResponseDateFresh(e) {
      if (this._config.maxAgeFrom === "last-used") {
        return !0;
      }
      const t = Date.now();
      if (!this._config.maxAgeSeconds) {
        return !0;
      }
      const s = this._getDateHeaderTimestamp(e);
      return s === null ? !0 : s >= t - this._config.maxAgeSeconds * 1e3;
    }
    _getDateHeaderTimestamp(e) {
      if (!e.headers.has("date")) {
        return null;
      }
      const t = e.headers.get("date"),
        s = new Date(t).getTime();
      return Number.isNaN(s) ? null : s;
    }
    async cacheDidUpdate({ cacheName: e, request: t }) {
      const s = this._getCacheExpiration(e);
      await s.updateTimestamp(t.url), await s.expireEntries();
    }
    async deleteCacheAndMetadata() {
      for (const [e, t] of this._cacheExpirations) {
        await self.caches.delete(e), await t.delete();
      }
      this._cacheExpirations = new Map();
    }
  },
  dt = "serwist-google-analytics",
  pt = 2880,
  gt = /^\/(\w+\/)?collect/,
  ft =
    (e) =>
    async ({ queue: t }) => {
      let s;
      while ((s = await t.shiftRequest())) {
        const { request: r, timestamp: a } = s,
          n = new URL(r.url);
        try {
          const o =
              r.method === "POST"
                ? new URLSearchParams(await r.clone().text())
                : n.searchParams,
            i = a - (Number(o.get("qt")) || 0),
            c = Date.now() - i;
          if ((o.set("qt", String(c)), e.parameterOverrides)) {
            for (const l of Object.keys(e.parameterOverrides)) {
              const h = e.parameterOverrides[l];
              o.set(l, h);
            }
          }
          typeof e.hitFilter == "function" && e.hitFilter.call(null, o),
            await fetch(
              new Request(n.origin + n.pathname, {
                body: o.toString(),
                credentials: "omit",
                headers: { "Content-Type": "text/plain" },
                method: "POST",
                mode: "cors",
              })
            );
        } catch (o) {
          throw (await t.unshiftRequest(s), o);
        }
      }
    },
  mt = (e) => {
    const t = ({ url: r }) =>
        r.hostname === "www.google-analytics.com" && gt.test(r.pathname),
      s = new O({ plugins: [e] });
    return [new d(t, s, "GET"), new d(t, s, "POST")];
  },
  wt = (e) => {
    const t = ({ url: s }) =>
      s.hostname === "www.google-analytics.com" &&
      s.pathname === "/analytics.js";
    return new d(t, new _({ cacheName: e }), "GET");
  },
  yt = (e) => {
    const t = ({ url: s }) =>
      s.hostname === "www.googletagmanager.com" && s.pathname === "/gtag/js";
    return new d(t, new _({ cacheName: e }), "GET");
  },
  Nt = (e) => {
    const t = ({ url: s }) =>
      s.hostname === "www.googletagmanager.com" && s.pathname === "/gtm.js";
    return new d(t, new _({ cacheName: e }), "GET");
  },
  Pe = ({ serwist: e, cacheName: t, ...s }) => {
    const r = m.getGoogleAnalyticsName(t),
      a = new Ne(dt, { maxRetentionTime: pt, onSync: ft(s) }),
      n = [Nt(r), wt(r), yt(r), ...mt(a)];
    for (const o of n) {
      e.registerRoute(o);
    }
  },
  Et = class {
    _fallbackUrls;
    _serwist;
    constructor({ fallbackUrls: e, serwist: t }) {
      (this._fallbackUrls = e), (this._serwist = t);
    }
    async handlerDidError(e) {
      for (const t of this._fallbackUrls) {
        if (typeof t == "string") {
          const s = await this._serwist.matchPrecache(t);
          if (s !== void 0) {
            return s;
          }
        } else if (t.matcher(e)) {
          const s = await this._serwist.matchPrecache(t.url);
          if (s !== void 0) {
            return s;
          }
        }
      }
    }
  };
var $e = class extends b {
  async _handle(e, t) {
    let s = [],
      r = await t.cacheMatch(e),
      a;
    if (!r) {
      try {
        r = await t.fetchAndCachePut(e);
      } catch (n) {
        n instanceof Error && (a = n);
      }
    }
    if (!r) {
      throw new u("no-response", { error: a, url: e.url });
    }
    return r;
  }
};
var _t = class extends d {
    constructor(e, t) {
      const s = ({ request: r }) => {
        const a = e.getUrlsToPrecacheKeys();
        for (const n of De(r.url, t)) {
          const o = a.get(n);
          if (o) {
            return { cacheKey: o, integrity: e.getIntegrityForPrecacheKey(o) };
          }
        }
      };
      super(s, e.precacheStrategy);
    }
  },
  vt = class {
    _precacheController;
    constructor({ precacheController: e }) {
      this._precacheController = e;
    }
    cacheKeyWillBeUsed = async ({ request: e, params: t }) => {
      const s =
        t?.cacheKey || this._precacheController.getPrecacheKeyForUrl(e.url);
      return s ? new Request(s, { headers: e.headers }) : e;
    };
  },
  bt = (e, t = {}) => {
    const {
      cacheName: s,
      plugins: r = [],
      fetchOptions: a,
      matchOptions: n,
      fallbackToNetwork: o,
      directoryIndex: i,
      ignoreURLParametersMatching: c,
      cleanURLs: l,
      urlManipulation: h,
      cleanupOutdatedCaches: g,
      concurrency: v = 10,
      navigateFallback: U,
      navigateFallbackAllowlist: y,
      navigateFallbackDenylist: p,
    } = t ?? {};
    return {
      precacheMiscOptions: {
        cleanupOutdatedCaches: g,
        concurrency: v,
        navigateFallback: U,
        navigateFallbackAllowlist: y,
        navigateFallbackDenylist: p,
      },
      precacheRouteOptions: {
        cleanURLs: l,
        directoryIndex: i,
        ignoreURLParametersMatching: c,
        urlManipulation: h,
      },
      precacheStrategyOptions: {
        cacheName: m.getPrecacheName(s),
        fallbackToNetwork: o,
        fetchOptions: a,
        matchOptions: n,
        plugins: [...r, new vt({ precacheController: e })],
      },
    };
  },
  Ie = class {
    _urlsToCacheKeys = new Map();
    _urlsToCacheModes = new Map();
    _cacheKeysToIntegrities = new Map();
    _concurrentPrecaching;
    _precacheStrategy;
    _routes;
    _defaultHandlerMap;
    _catchHandler;
    _requestRules;
    constructor({
      precacheEntries: e,
      precacheOptions: t,
      skipWaiting: s = !1,
      importScripts: r,
      navigationPreload: a = !1,
      cacheId: n,
      clientsClaim: o = !1,
      runtimeCaching: i,
      offlineAnalyticsConfig: c,
      disableDevLogs: l = !1,
      fallbacks: h,
      requestRules: g,
    } = {}) {
      const {
        precacheStrategyOptions: v,
        precacheRouteOptions: U,
        precacheMiscOptions: y,
      } = bt(this, t);
      if (
        ((this._concurrentPrecaching = y.concurrency),
        (this._precacheStrategy = new ve(v)),
        (this._routes = new Map()),
        (this._defaultHandlerMap = new Map()),
        (this._requestRules = g),
        (this.handleInstall = this.handleInstall.bind(this)),
        (this.handleActivate = this.handleActivate.bind(this)),
        (this.handleFetch = this.handleFetch.bind(this)),
        (this.handleCache = this.handleCache.bind(this)),
        r && r.length > 0 && self.importScripts(...r),
        a && xe(),
        n !== void 0 && qe({ prefix: n }),
        s
          ? self.skipWaiting()
          : self.addEventListener("message", (p) => {
              p.data && p.data.type === "SKIP_WAITING" && self.skipWaiting();
            }),
        o && re(),
        e && e.length > 0 && this.addToPrecacheList(e),
        y.cleanupOutdatedCaches && se(v.cacheName),
        this.registerRoute(new _t(this, U)),
        y.navigateFallback &&
          this.registerRoute(
            new be(this.createHandlerBoundToUrl(y.navigateFallback), {
              allowlist: y.navigateFallbackAllowlist,
              denylist: y.navigateFallbackDenylist,
            })
          ),
        c !== void 0 &&
          (typeof c == "boolean"
            ? c && Pe({ serwist: this })
            : Pe({ ...c, serwist: this })),
        i !== void 0)
      ) {
        if (h !== void 0) {
          const p = new Et({ fallbackUrls: h.entries, serwist: this });
          i.forEach(($) => {
            $.handler instanceof b &&
              !$.handler.plugins.some((Le) => "handlerDidError" in Le) &&
              $.handler.plugins.push(p);
          });
        }
        for (const p of i) {
          this.registerCapture(p.matcher, p.handler, p.method);
        }
      }
      l && me();
    }
    get precacheStrategy() {
      return this._precacheStrategy;
    }
    get routes() {
      return this._routes;
    }
    addEventListeners() {
      self.addEventListener("install", this.handleInstall),
        self.addEventListener("activate", this.handleActivate),
        self.addEventListener("fetch", this.handleFetch),
        self.addEventListener("message", this.handleCache);
    }
    addToPrecacheList(e) {
      const t = [];
      for (const s of e) {
        typeof s == "string"
          ? t.push(s)
          : s && !s.integrity && s.revision === void 0 && t.push(s.url);
        const { cacheKey: r, url: a } = Se(s),
          n = typeof s != "string" && s.revision ? "reload" : "default";
        if (
          this._urlsToCacheKeys.has(a) &&
          this._urlsToCacheKeys.get(a) !== r
        ) {
          throw new u("add-to-cache-list-conflicting-entries", {
            firstEntry: this._urlsToCacheKeys.get(a),
            secondEntry: r,
          });
        }
        if (typeof s != "string" && s.integrity) {
          if (
            this._cacheKeysToIntegrities.has(r) &&
            this._cacheKeysToIntegrities.get(r) !== s.integrity
          ) {
            throw new u("add-to-cache-list-conflicting-integrities", {
              url: a,
            });
          }
          this._cacheKeysToIntegrities.set(r, s.integrity);
        }
        this._urlsToCacheKeys.set(a, r), this._urlsToCacheModes.set(a, n);
      }
      if (t.length > 0) {
        const s = `Serwist is precaching URLs without revision info: ${t.join(", ")}
This is generally NOT safe. Learn more at https://bit.ly/wb-precache`;
        console.warn(s);
      }
    }
    handleInstall(e) {
      return (
        this.registerRequestRules(e),
        V(e, async () => {
          const t = new Ce();
          this.precacheStrategy.plugins.push(t),
            await Oe(
              this._concurrentPrecaching,
              Array.from(this._urlsToCacheKeys.entries()),
              async ([a, n]) => {
                const o = this._cacheKeysToIntegrities.get(n),
                  i = this._urlsToCacheModes.get(a),
                  c = new Request(a, {
                    cache: i,
                    credentials: "same-origin",
                    integrity: o,
                  });
                await Promise.all(
                  this.precacheStrategy.handleAll({
                    event: e,
                    params: { cacheKey: n },
                    request: c,
                    url: new URL(c.url),
                  })
                );
              }
            );
          const { updatedURLs: s, notUpdatedURLs: r } = t;
          return { notUpdatedURLs: r, updatedURLs: s };
        })
      );
    }
    async registerRequestRules(e) {
      if (this._requestRules && e?.addRoutes) {
        try {
          await e.addRoutes(this._requestRules), (this._requestRules = void 0);
        } catch (t) {
          throw t;
        }
      }
    }
    handleActivate(e) {
      return V(e, async () => {
        const t = await self.caches.open(this.precacheStrategy.cacheName),
          s = await t.keys(),
          r = new Set(this._urlsToCacheKeys.values()),
          a = [];
        for (const n of s) {
          r.has(n.url) || (await t.delete(n), a.push(n.url));
        }
        return { deletedCacheRequests: a };
      });
    }
    handleFetch(e) {
      const { request: t } = e,
        s = this.handleRequest({ event: e, request: t });
      s && e.respondWith(s);
    }
    handleCache(e) {
      if (e.data && e.data.type === "CACHE_URLS") {
        const { payload: t } = e.data,
          s = Promise.all(
            t.urlsToCache.map((r) => {
              let a;
              return (
                typeof r == "string"
                  ? (a = new Request(r))
                  : (a = new Request(...r)),
                this.handleRequest({ event: e, request: a })
              );
            })
          );
        e.waitUntil(s),
          e.ports?.[0] && s.then(() => e.ports[0].postMessage(!0));
      }
    }
    setDefaultHandler(e, t = "GET") {
      this._defaultHandlerMap.set(t, q(e));
    }
    setCatchHandler(e) {
      this._catchHandler = q(e);
    }
    registerCapture(e, t, s) {
      const r = Te(e, t, s);
      return this.registerRoute(r), r;
    }
    registerRoute(e) {
      this._routes.has(e.method) || this._routes.set(e.method, []),
        this._routes.get(e.method).push(e);
    }
    unregisterRoute(e) {
      if (!this._routes.has(e.method)) {
        throw new u("unregister-route-but-not-found-with-method", {
          method: e.method,
        });
      }
      const t = this._routes.get(e.method).indexOf(e);
      if (t > -1) {
        this._routes.get(e.method).splice(t, 1);
      } else {
        throw new u("unregister-route-route-not-registered");
      }
    }
    getUrlsToPrecacheKeys() {
      return this._urlsToCacheKeys;
    }
    getPrecachedUrls() {
      return [...this._urlsToCacheKeys.keys()];
    }
    getPrecacheKeyForUrl(e) {
      const t = new URL(e, location.href);
      return this._urlsToCacheKeys.get(t.href);
    }
    getIntegrityForPrecacheKey(e) {
      return this._cacheKeysToIntegrities.get(e);
    }
    async matchPrecache(e) {
      const t = e instanceof Request ? e.url : e,
        s = this.getPrecacheKeyForUrl(t);
      if (s) {
        return (await self.caches.open(this.precacheStrategy.cacheName)).match(
          s
        );
      }
    }
    createHandlerBoundToUrl(e) {
      const t = this.getPrecacheKeyForUrl(e);
      if (!t) {
        throw new u("non-precached-url", { url: e });
      }
      return (s) => (
        (s.request = new Request(e)),
        (s.params = { cacheKey: t, ...s.params }),
        this.precacheStrategy.handle(s)
      );
    }
    handleRequest({ request: e, event: t }) {
      const s = new URL(e.url, location.href);
      if (!s.protocol.startsWith("http")) {
        return;
      }
      let r = s.origin === location.origin,
        { params: a, route: n } = this.findMatchingRoute({
          event: t,
          request: e,
          sameOrigin: r,
          url: s,
        }),
        o = n?.handler,
        i = [],
        c = e.method;
      if (
        (!o &&
          this._defaultHandlerMap.has(c) &&
          (o = this._defaultHandlerMap.get(c)),
        !o)
      ) {
        return;
      }
      let l;
      try {
        l = o.handle({ event: t, params: a, request: e, url: s });
      } catch (g) {
        l = Promise.reject(g);
      }
      const h = n?.catchHandler;
      return (
        l instanceof Promise &&
          (this._catchHandler || h) &&
          (l = l.catch(async (g) => {
            if (h) {
              try {
                return await h.handle({
                  event: t,
                  params: a,
                  request: e,
                  url: s,
                });
              } catch (v) {
                v instanceof Error && (g = v);
              }
            }
            if (this._catchHandler) {
              return this._catchHandler.handle({
                event: t,
                request: e,
                url: s,
              });
            }
            throw g;
          })),
        l
      );
    }
    findMatchingRoute({ url: e, sameOrigin: t, request: s, event: r }) {
      const a = this._routes.get(s.method) || [];
      for (const n of a) {
        let o,
          i = n.match({ event: r, request: s, sameOrigin: t, url: e });
        if (i) {
          return (
            (o = i),
            ((Array.isArray(o) && o.length === 0) ||
              (i.constructor === Object && Object.keys(i).length === 0) ||
              typeof i == "boolean") &&
              (o = void 0),
            { params: o, route: n }
          );
        }
      }
      return {};
    }
  };
var P = (e) =>
    new Ue({
      maxAgeFrom: "last-used",
      maxAgeSeconds: 336 * 60 * 60,
      maxEntries: e,
    }),
  Rt = [
    {
      handler: new $e({ cacheName: "next-static", plugins: [P(128)] }),
      matcher: ({ sameOrigin: e, url: t }) =>
        e && t.pathname.startsWith("/_next/static/"),
    },
    {
      handler: new O(),
      matcher: ({ sameOrigin: e, url: t }) =>
        e && (t.pathname.startsWith("/r/") || t.pathname.startsWith("/api/")),
    },
    {
      handler: new _({
        cacheName: "pages-rsc",
        matchOptions: { ignoreSearch: !0 },
        networkTimeoutSeconds: 3,
        plugins: [P(400)],
      }),
      matcher: ({ request: e, sameOrigin: t, url: s }) =>
        t && (e.headers.get("RSC") === "1" || s.pathname.endsWith(".txt")),
    },
    {
      handler: new _({
        cacheName: "pages",
        matchOptions: { ignoreSearch: !0 },
        networkTimeoutSeconds: 3,
        plugins: [P(400)],
      }),
      matcher: ({ request: e, sameOrigin: t }) =>
        t && (e.mode === "navigate" || e.destination === "document"),
    },
    {
      handler: new _({
        cacheName: "same-origin",
        networkTimeoutSeconds: 3,
        plugins: [P(64)],
      }),
      matcher: ({ request: e, sameOrigin: t, url: s }) =>
        t && e.method === "GET" && !s.pathname.startsWith("/r/"),
    },
  ],
  xt = new Ie({
    clientsClaim: !0,
    disableDevLogs: !0,
    fallbacks: {
      entries: [
        {
          matcher: ({ request: e }) => e.destination === "document",
          url: "/~offline",
        },
      ],
    },
    navigationPreload: !0,
    precacheEntries: N,
    precacheOptions: { cleanupOutdatedCaches: !0, concurrency: 10 },
    runtimeCaching: Rt,
    skipWaiting: !1,
  });
xt.addEventListeners();
